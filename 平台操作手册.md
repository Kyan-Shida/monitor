# 智能机器巡检平台 · 操作手册

> **适用环境**：一期演示环境（纯前端原型，本机运行）
> **文档版本**：v1.1 ｜ **更新日期**：2026-09-26
> **面向读者**：客户方业务用户 · 首次使用者 · 管理者（领导）· 研发与实施人员

---

## 阅读指引（按角色）

| 你是谁 | 建议阅读顺序 | 可以跳过 |
|--------|-------------|---------|
| 客户方业务用户 | 第一章（概览）→ 第二章（快速上手）→ 第三章里你负责的模块 → 第四章（业务流程剧本） | 第五、六、九章 |
| 第一次使用平台的人 | 第二章（快速上手）→ 第三章（模块操作）→ 第九章 FAQ | 第五章权限细节 |
| 管理者 / 领导 | 第一章（概览 + 演示边界）→ 第三章 3.1 工作台 / 3.6 指标 / 3.8 驾驶舱 → 第四章（流程是怎么闭环的）→ 第六章（状态与规则速查）→ 第八章（操作日志与追溯） | 第三章的配置类模块 |
| 研发 / 实施人员 | 第一章 1.5 术语表 → 第五章（角色权限）→ 第六章（状态机与校验规则）→ 第七章（数据与交付）→ 第九章附录（页面地图） | — |

---

## 一、平台概览

### 1.1 一句话定位

**把「机器人巡检」当成一条可追溯的业务流水线来管**：从地图与点位、巡检计划、任务调度、机器人执行，到结果复核、异常告警、复查闭环，全流程在一个平台上完成，并且每一环都能查到"谁在什么时候做了什么、依据是什么"。

### 1.2 平台能做什么

| 能力 | 说明 | 主要页面 |
|------|------|---------|
| 机器人资产管理 | 机器台账、运行监测、能力与健康、地图版本、机型差异 | 机器人管理 |
| 地图与点位管理 | 地图导入与版本发布、点位标注与验证、现场变化登记与修订 | 资源与地图 |
| 巡检计划与任务 | 模板 → 计划 → 任务，支持定时生成与临时任务 | 巡检执行 |
| 任务调度 | 按能力/区域/电量/版本校验可用机器人，派单入列或立即执行 | 巡检执行 › 任务调度工作台 |
| 执行监控 | 实时看阶段推进、地图位置、实时结果；异常可暂停/接管/终止/重试 | 巡检执行 › 实时执行监控 |
| 结果与复核 | 按任务维度查结果，人工复核（确认 / 修正 / 标记无效） | 结果与异常 |
| 异常与告警闭环 | 告警确认 → 创建复查任务 → 复查恢复 → 关闭 | 结果与异常 › 告警 |
| 追溯与审计 | 执行回溯（过程回放）、复核历史、处置轨迹、审计日志 | 结果与异常 / 系统设置 |
| 指标与驾驶舱 | 26 项指标统一口径 + 大屏看板 | 分析与报表 / 驾驶舱大屏 |

### 1.3 演示环境说明（**必读**）

> 这也是交付时最容易被误解的地方，请先看完这一段。

| 事项 | 说明 |
|------|------|
| 是否有后端 | **没有**。纯前端演示程序，所有数据来自本机内置的演示数据。 |
| 是否控制真实机器人 | **否**。所有"下发 / 遥控 / 急停"只在界面上模拟，不会发出任何真实指令。 |
| 数据存在哪 | 浏览器本机存储（`localStorage`）。换浏览器、换电脑、清缓存都会回到初始演示数据。 |
| 界面上的"演示"字样 | 页面与页脚统一使用「演示环境 / 演示数据」，这是**刻意标注**，用于区分演示与生产。 |
| 操作会不会"做坏"数据 | 会改动本机演示数据。**任何时候都可以一键恢复**：顶栏「重置演示数据」→「确认重置」。 |
| 异常提示 | 操作不满足业务规则时（如机器人电量低于门槛、地图版本不一致），界面会弹出失败提示——这是**设计好的校验**，不是程序报错。 |

### 1.4 账号与角色

演示环境用「用户菜单」代替真实登录：点击顶栏右上角的**头像按钮（姓名 + 角色）**展开下拉菜单，菜单内「切换为…」完成角色切换，底部「退出登录」在演示环境中仅作提示（无真实登录态）：

| 角色 | 演示人员 | 可见范围 | 数据范围 | 默认落地页 | 驾驶舱默认视图 |
|------|---------|---------|---------|-----------|--------------|
| 管理员 | 王姐 | 全部 7 个一级中心（含**系统设置**） | 全租户 | 我的工作台 | 综合总览 |
| 非管理员 | 老周 | 6 个一级中心（**不含系统设置**） | 分管厂区 | 我的工作台 | 运营管理 |

- 管理员独有：配置编辑 / 发布、组织编辑、权限编辑、审计查看、导出审批、口径管理、接管审批。
- 切换角色后会跳回该角色的默认落地页，菜单与部分按钮随之变化。
- 权限不足的页面直接打开 URL 时，会显示权限提示而不是空白页。

### 1.5 界面通用元素

| 元素 | 位置 | 怎么用 |
|------|------|--------|
| 侧边栏一级中心 | 左侧 | 点一次展开、再点收起；7 个中心：工作台 / 巡检执行 / 结果与异常 / 资源与地图 / 机器人管理 / 分析与报表 / 系统设置 |
| 侧边栏搜索 | 左侧顶部 | 输入关键词快速定位页面（已预置常用业务词，如"复核""告警"） |
| 中心 Tab（二级导航） | 页面顶部 | 在同一中心内切换业务页，带二级分组标签（标准与模板 / 任务与计划 / 调度 / 执行…） |
| 面包屑 + 返回 | 内容区顶部 | 面包屑显示"中心 › 页 › 详情"，**返回按钮按业务层级回退**，不是浏览器历史 |
| KPI 卡 | 各页顶部 | 点数字看明细或下钻（工作台点 KPI 打开明细弹窗） |
| 面板 / 表格 | 内容区 | 表格列头即字段；行内操作列是唯一的操作入口 |
| 徽标（Badge） | 各列内 | 表示状态 / 等级 / 优先级，颜色：红=异常或紧急，琥珀=高或待办，绿=正常或空闲 |
| 弹窗（Modal） | 居中 | 用于"一次填写一次提交"的操作；**演示环境中的弹窗都是模拟提交** |
| 步骤条（Steps） | 页面内 | 表示业务当前走到第几步（如告警处置、接管流程） |
| 对象引用 | 表格 / 卡片内 | 蓝色带下划线的编号可点击下钻；普通文字为**纯展示**（不跳转） |
| 分页 | 列表底部 | 显示"共 N 条 · 第 x / y 页" |
| 视图记忆 | 全局 | 筛选条件、分页、Tab 会在本标签页会话内保留，切页返回不丢 |

### 1.6 术语表

| 术语 | 含义 |
|------|------|
| 地图 / 点位 | 机器人作业的"底图"与"要看的位置"。地图有**版本号**，点位有**点位集合版本号**（界面显示 `m3 / p7`） |
| 检测对象 | 设备上的一个被检目标（如"V001 出口压力表"） |
| 检测项 | 对象上要读的一个量（如压力读数 / 阀位 / 罐壁温度），含单位与合格要求 |
| 地图状态 | 草稿 → 试验发布 → 已发布（只有已发布版本才允许正式同步给机器人） |
| 点位状态 | 待标注 → 待验证 → 已启用（还有：待重新验证 / 停用） |
| 任务类型 | 光学任务 / 红外任务 / 气体采集任务 / 声音采集任务 / 综合巡检任务 |
| 原子动作 | 拍照 / 录像片段 / 采集红外热像 / 采集气体 / 录制声音；调度时按机器人**能力**校验 |
| 执行阶段 | 单次作业的 7 段：前往目标区域 → 搜索目标 → 目标锁定 → 自主调整 → 数据采集 → 本地处理 / AI 分析 → 结果生成 |
| 识别值 → 最终值 | 机器人采集并识别出的值 → 复核后的最终业务值（不修正时两者相同） |
| 复核 | 人对结果做确认 / 修正 / 标记无效，写审计记录 |
| 复查 | 异常后**再跑一次**该点位的巡检，用于确认问题是否恢复（由告警页创建） |
| 工单 / SLA | 告警转人工处置的工单，带处置时限（SLA），超时会预警与升级 |
| 接管 | 人临时接管机器人手动遥控，完成后需核对版本、任务断点、结果一致性再释放 |
| 版本同步 | 把地图/点位版本下发给机器人：传输中 → 校验中 → 待激活 → 已同步 |

---

## 二、快速上手（15 分钟跑通全流程）

### 2.1 打开平台

| 方式 | 操作 |
|------|------|
| 演示机（开发服务） | 访问 `http://localhost:5173` |
| 离线单文件（发给客户） | 双击 `静态版/index.html`（自包含，无需联网、无需安装） |

打开后默认进入 **我的工作台**。

### 2.2 界面 5 分钟认路

1. **左侧**：7 个一级中心，点一下展开。
2. **顶部**：中心 Tab（二级导航）。
3. **右上角**：角色切换器、**消息中心铃铛**（点击弹出消息中心弹窗：全部 / 系统通知 / 业务消息 / 告警预警分类 Tab，支持「全部已读 / 清空」；告警类消息点开直达告警事件页，待复核 / 待调度类直达对应页面；铃铛角标显示未读数，已读与清空状态保存在本机）、重置演示数据、驾驶舱大屏。
4. **内容区**：KPI 卡 → 面板 / 表格 → 行内操作按钮。
5. **内容区顶部**：面包屑 + 返回。

### 2.3 一条主流程走到底（建议第一次就这么走）

| 步 | 你在哪 | 做什么 | 会看到什么 |
|----|--------|--------|-----------|
| 1 | 我的工作台 | 看 6 个 KPI（待复核结果 / 待确认告警 / 待调度任务 / 机器人待关注 / 即将超期 / 执行中任务） | 顶部即全局待办数量 |
| 2 | 我的工作台 | 点「待复核结果」 | 弹出该类待办明细（标题带数量），底部可「结果查询」 |
| 3 | 待办弹窗 | 点某条的「去复核」 | 跳到「结果详情 / 复核」页 |
| 4 | 结果详情 / 复核 | 看证据、原始值 → 识别值 → 最终值，在「人工复核」填写结论与依据，点「提交复核」 | 提交成功提示，结果状态更新，工作台待复核数 -1 |
| 5 | 我的工作台 | 点「待确认告警」→ 底部「告警事件」 | 进入告警事件列表 |
| 6 | 告警事件 | 点某条「处置 / 复查」 | 进入告警详情 / 复查页，顶部步骤条显示所处阶段 |
| 7 | 告警详情 / 复查 | 点「确认告警」→ 填处置说明 →「保存处置记录」 | 告警从「待确认」→「处理中」，写入处置轨迹 |
| 8 | 告警详情 / 复查 | 点「创建复查任务」 | 自动生成一条「XX复查」任务并跳到调度工作台 |
| 9 | 任务调度工作台 | 左选任务、右选机器人，派单方式选「立即执行」，点「立即派单执行」 | 任务进入执行（或按机器人忙闲入队），顶部提示成功 |
| 10 | 实时执行监控 | 看阶段推进、地图位置、实时结果；可「演示事件注入」模拟异常或阶段回报 | KPI 与实时结果表实时变化 |
| 11 | 结果与异常 › 巡检结果查询 | 按任务分组展开，看这次任务的全部检测结果 | 每任务一行，点开才是明细 |
| 12 | 结果与异常 › 执行回溯记录 | 选任务看执行事件与证据时间轴 | 谁在什么时候采集了什么、读数多少、配现场图 |

> 走完这 12 步，你已经覆盖了平台 80% 的核心操作。

### 2.4 通用交互约定

- **看明细**：列表点行内按钮（详情 / 证据 / 复核 / 处置），不要期待点整行。
- **改数据**：只有明确标注动作的按钮（提交 / 保存 / 派单 / 确认）才会改动演示数据，其他都是查看。
- **筛选与重置**：列表页顶部筛选条，右侧通常有「重置筛选」。
- **页签导航**：面包屑下方有一条**仿浏览器多标签**的页签条。每打开一个页面自动生成一页签，点页签即可在多个功能页之间快速切换，不必回左侧菜单重新点；页签带「×」可关闭，右侧「⌄」菜单提供「关闭其他页签 / 关闭全部页签」。「工作台」为常驻页签不可关闭；切换角色后，当前角色无权限的页签会自动移除；页签记录随浏览器会话保留，刷新后仍在。
- **工作台顶部**：进入工作台首先看到**欢迎横幅**（左侧 3D 形象问候语，含当前日期与待办总数）和右侧**快捷入口**（新建任务 / 任务调度 / 结果复核 / 告警记录四个常用模块），点图标即可一键直达；快捷入口按当前角色权限自动增减，无权限的入口不会显示。工作台整屏铺满：KPI 卡片下方为两块实时面板**「进行中任务」（含进度条）**与**「机器人运行概览」（状态点标识在线 / 离线）**，撑满剩余高度且各自滚动，点条目可跳转对应监控页。
- **跑错了**：顶栏「重置演示数据」→「确认重置」，一切回到初始状态。

### 2.5 按岗位的快速上手清单

- **管理者 / 领导**：先读第一章演示边界 → 工作台看全局待办（3.1）→ 指标中心看趋势（3.6）→ 管理驾驶舱做汇报（3.8）→ 异常闭环看第四章流程 D。重点关心"待办有没有清零、闭环率、MTTR"。
- **巡检调度岗**：计划列表生成任务（3.2.2）→ 任务调度工作台派单（3.2.4）→ 机器人任务队列调整（3.2.5）→ 实时执行监控看推进（3.2.6）。派单被拒看 6.7。
- **结果复核岗**：工作台「待复核结果」→ 去复核（3.1 / 3.3.2）→ 按任务查结果（3.3.1）。修正 / 标记无效会影响任务状态（见 3.3.2）。
- **设备 / 现场岗**：设备资源树查设备档案（3.4.1 / 3.3.3）→ 点位标注与验证（3.4.3）→ 现场变化登记与修订（3.4.4 流程 E）。
- **实施 / 研发**：角色权限（第五章）→ 状态机与校验规则（第六章）→ 数据与离线交付（第七章）→ 页面地图与跳转（第九章）。

---

## 三、模块操作说明

### 3.0 模块总览

| 一级中心 | 页面 | 主要用途 |
|---------|------|---------|
| 工作台 | 我的工作台 | 待办总入口（6 个 KPI → 明细弹窗） |
| 巡检执行 | 巡检模板 / 计划列表 / 计划编辑 / 任务列表 | 定义"要巡检什么、多久一次、生成任务" |
| 巡检执行 | 任务调度工作台 / 机器人任务队列 | 把任务派给机器人、排队与调整 |
| 巡检执行 | 实时执行监控 / 执行回溯记录 | 看执行过程、控制任务、回看过程 |
| 结果与异常 | 巡检结果查询 / 结果详情·复核 / 设备巡检档案 | 查结果、复核、按设备看档案 |
| 分析与报表 | 指标中心 / 巡检结果报表 | 看统一口径指标、按任务预览 / 导出巡检结果报表 |
| 结果与异常 | 告警事件 / 告警详情·复查 | 异常确认、复查与关闭 |
| 资源与地图 | 设备资源树 / 巡检点列表 / 点位标注与验证工作台 / 地图管理 | 设备台账、点位、地图版本 |
| 机器人管理 | 机器台账 / 运行监测 / 能力与健康 / 地图与版本 / 人工操作与遥控 / 机型专项 | 机器人全生命周期 |
| 分析与报表 | 指标中心 | 26 项指标统一口径 + 下钻 |
| 系统设置 | 角色 / 权限、审计日志、接口配置、资源库、字典参数 | 管理与集成（**管理员可见**） |
| （独立入口） | 驾驶舱大屏 | 管理驾驶舱 / 调度指挥驾驶舱 |

---

### 3.1 我的工作台

**用途**：每天上班第一眼看这里，所有待办都在这。

**界面**：顶部 6 个可点击 KPI；点击后在弹窗中展开该类待办的明细（页面本身只读，不做任何写操作）。

| 操作 | 结果 |
|------|------|
| 点「待复核结果」 | 弹窗列出待复核结果（设备 · 对象 · 检测项、识别值 → 最终值、置信度、采集时间、来源任务）；每条带「去复核」→ 结果详情 / 复核页 |
| 点「待确认告警」 | 弹窗列出待确认告警（名称 + 等级 + 点位 + 时间）；底部「告警事件」进列表 |
| 点「待调度任务」 | 弹窗列出待调度任务（来源 / 地图 / 点位数 / 预计耗时 / 要求完成时间）；底部「调度工作台」 |
| 点「机器人待关注」 | 弹窗列出需要关注的机器人及原因（通信离线 / 电量低于门槛 / 地图版本落后），附区域、电量、能力 |
| 点「即将超期」 | 弹窗列出距完成时限不足 15 分钟且未终结的任务；底部「任务列表」 |
| 点「执行中任务」 | 弹窗列出执行中任务（机器人 / 完成度 / 当前阶段 / 当前点位）；底部「实时执行监控」 |

**注意**：KPI 数字与明细条数同源同口径（都取自指标中心），不会出现"数字 3 条、点开 5 条"。

---

### 3.2 巡检执行

#### 3.2.1 巡检模板（`巡检模板`）

定义"巡检哪些点位、什么优先级、要求什么时候完成"。

| 操作 | 结果 |
|------|------|
| ＋ 新建模板 | 打开「新建模板」弹窗：填名称、优先级、完成时限，勾选点位清单 →「保存模板」 |
| 行内「编辑」 | 打开「编辑模板」，保存后模板版本 +1 |
| 行内「停用 / 启用」 | 切换模板状态。**停用后不参与计划生成**，仅存档 |
| 行内「删除」 | 二次确认后删除 |

#### 3.2.2 计划列表 / 计划编辑

计划 = 模板 + 周期 + 时间 + 提前量 + 超时 / 等待参数。

| 操作 | 结果 |
|------|------|
| ＋ 新建计划 | 打开计划弹窗，选择模板与周期参数 →「保存计划版本」 |
| 行内「编辑」 | 进入「计划编辑」页（9 项参数），「保存计划版本」后回列表；「取消」直接返回 |
| 行内「模拟自动下发计划任务」 | 按该计划+模板**生成一条待调度任务**，页面自动跳到任务列表 |

#### 3.2.3 任务列表

| 操作 | 结果 |
|------|------|
| 筛选 | 来源 / 优先级 / 状态 / 机器人 + 关键词搜索 |
| ＋ 临时巡检任务 | 打开「临时任务」弹窗（填名称、选点位、优先级等）→「进入调度」：生成待调度任务并跳到调度工作台 |
| 行内「详情」 | 进入任务详情（含检测项快照） |
| 任务快照面板「查看任务结果」 | 跳到巡检结果查询，**只展示该任务**的结果 |

#### 3.2.4 任务调度工作台

**这是最核心的操作页**：左边选任务、右边选机器人、下面点派单。

| 区域 | 操作 |
|------|------|
| 顶部 KPI | 待调度 / 高·紧急 / 即将超期 / 执行中 / 已排队 / 空闲机器人 / 版本待同步（带口径提示） |
| 待调度任务池 | 搜索、点选任务、卡内「详情」 |
| 机器人资源池 | 点选机器人、卡内「详情」 |
| 调度决策 | 派单方式二选一：**加入队列** 或 **立即执行**；主按钮「派单入列 / 立即派单执行」 |
| 未来任务时间轴 | 切未来 4 / 8 小时，看各机器人占用情况 |
| 任务分配与接收记录 | 查看调度决策、跳「机器人队列」；时间轴摘要弹窗内可「任务详情 / 执行监控」 |

**派单结果**：机器人空闲 → 任务直接进入「执行中」；机器人忙 → 任务进入「待执行」（排队），可在队列页调整。

**派单会被拒绝的情况**（界面给提示）：区域不匹配 / 机器人不具备该检测能力 / 不具备要求的移动能力（爬楼、越障等）/ 电量低于机型门槛 / 轨道区段被占用 / 机器人离线或故障 / 地图或点位版本与任务快照不一致 / 已有人工接管会话 / 点位被停用 / 机器人当前已有任务。

#### 3.2.5 机器人任务队列

| 操作 | 结果 |
|------|------|
| 页头选机器人 | 切换查看对象；可切未来 4 / 8 小时 |
| 拖拽排序 | 调整排队顺序 |
| 「恢复任务 / 开始任务」 | 任务开始执行 |
| 「暂停任务」 | 任务转「暂停」（保留断点） |
| 「改派」 | 弹「撤回确认与改派」→「确认撤回并改派」，任务改给别的机器人并回到「已分配」 |
| 「置于队首」 | 插到队首 |
| 「任务抢占」 | 弹「确认抢占影响」→「模拟暂停确认并抢占」：暂停当前执行任务、新任务插入 |
| 「移除出列」 | 任务退回「待调度」，清空机器人 |

#### 3.2.6 实时执行监控

| 操作 | 结果 |
|------|------|
| 顶部任务选择 | 切换要监控的任务 |
| 地图点选 | 点机器人 → 机器人详情；点点位 → 点位详情 |
| ＋ 下发临时任务 | 弹窗填名称 / 优先级 / 任务类型 / 目标机器人 / 原子动作 / 点位，看「下发影响」预览 →「创建并下发」，直接下发到指定机器人 |
| 暂停 / 恢复任务 | 任务转「暂停」/ 回到「执行中」 |
| 人工接管 → | 跳到「人工操作与遥控」页 |
| 终止任务 | 任务结束（部分完成或取消） |
| 重试目标 / 跳过检测项 | 任务出现失败原因后才可用：重试当前目标，或跳过该项继续 |
| 演示事件注入 | 勾选「生成异常结果」后「模拟下一自主阶段回报」可产出异常；也可选 6 类失败原因、或「模拟 TASK_FINISHED」直接收尾 |
| 提交临时任务后的提示 | 「查看调度详情」/「查看机器人队列」 |

#### 3.2.7 执行回溯记录

**这是平台的"操作日志"主入口**：按任务回看执行全过程。

| 区域 | 操作 |
|------|------|
| 左列 | 搜索、按状态筛选、时间范围（近 7 天 / 近 30 天 / 全部 / 自定义起止） |
| 列表 | 点任务看下方时间轴 |
| 右列 | 执行事件与证据时间轴：下发 → 开始 → 逐项采集回执（含**真实读数与现场图**）→ 结束/失败原因 |

---

### 3.3 结果与异常

#### 3.3.1 巡检结果查询

| 操作 | 结果 |
|------|------|
| 筛选 | 关键词（设备 / 对象 / 检测项）、**任务筛选**、**机器筛选**、结果状态、业务判断（正常 / 异常）、采集日期 +「重置筛选」 |
| 任务分组行 | 每行一个任务，显示：任务名 + 任务状态 + **任务判断（正常 / 异常 N）** + 任务号 / 机器人 / 结束时间 + 检测 N 项 / 待复核 N。异常任务左侧有红色标记条 |
| 「展开 / 收起」 | 展开后显示该任务的检测项明细表 |
| 「全部展开 / 全部收起」 | 一键展开全部任务明细 |
| 行内「证据 / 复核」 | 进入该结果的结果详情 / 复核页 |
| 分页 | 按**任务数**分页（每页 10 个任务） |

> 设计要点：任务多的时候列表只增长"行"，明细按需展开，页面高度不会失控。

#### 3.3.2 巡检结果报表（分析与报表 › 巡检报表）

专门承载「预览 / 导出」能力，与巡检结果查询页（侧重逐条复核）分离：

| 操作 | 结果 |
|------|------|
| 筛选 | 关键词（任务 / 编号 / 机器人）、**任务筛选**、**任务状态**（默认"已完成"） |
| 报表行 | 每已完成任务一行：任务名 + 任务号 + 机器人 + 结束时间 + 检测项数 + 异常数 + 任务状态 |
| 行内「预览」 | 弹窗预览该任务的「巡检结果报告」文档：报告头 + 元信息（机器人 / 结束 / 生成时间 / 数据范围）+ 概览统计（检测项 / 正常 / 异常 / 状态）+ 点位检测明细 + 页脚；可一键跳完整列表 |
| 行内勾选 + 顶部「导出 PDF / 导出 Word」 | 每行可勾选任务（支持单选一个 / 多选多个，或「全选当前」/「清空」），点导出按勾选范围生成报告；未勾选则导出当前筛选全部。演示环境为占位提示，弹窗列出将要导出的任务清单 |

#### 3.3.3 结果详情 / 复核

| 区域 | 操作 |
|------|------|
| 顶部 | 结果 ID、判断与状态、设备 / 点位 / 任务 / 机器人、采集时间；「任务过程回放 →」跳执行回溯 |
| 证据与值链路 | 原始采集值 → 机器人本地识别 → 外部 AI 识别值（含置信度）→ 平台规则判断依据 → 最终值 |
| 人工复核 | **复核结论**（确认 / 修正 / 标记无效）、**最终值**（选"修正"才可填）、**最终业务状态**（正常 / 异常）、**复核依据 / 无效原因（必填）** →「提交复核」 |

**提交后的连锁反应**：结果状态更新、追加一条复核审计记录；若判「标记无效」，该检测项从任务有效项中剔除，任务状态会重新计算（可能从"完成"变成"部分完成 / 失败"）。

#### 3.3.4 设备巡检档案

**按设备维度看全部历史**，5 个 Tab：

| Tab | 内容与操作 |
|-----|-----------|
| 检测对象 | 该设备的对象 / 检测项 / 点位 / 最新结果；结果编号可点 → **结果详情内置页**；下方「设备历史趋势」选点位看趋势 |
| 历史结果 | 该设备全部历史结果；结果编号可点 → 结果详情 |
| 关联告警 | 该设备相关告警；处置列「查看详情」→ **告警详情内置页** |
| 采集证据 | 按证据维度看（含来源、证据详情 → 结果详情） |
| 巡检任务 | 该设备的巡检任务；筛选：来源 / 机器人 / 状态 / 时间区间 / 关键词；已结束任务出现「过程回放」→ **执行回溯内置页** |

> 三个内置页（结果详情 / 告警详情 / 执行回溯）**只读**，都挂在"设备巡检档案"目录下：面包屑、返回按钮、侧边栏高亮都在档案页，不会跳到别的模块。

#### 3.3.5 告警事件

| 操作 | 结果 |
|------|------|
| 状态筛选 | 全部 / 待确认 / 处理中 / 待复查 / 已恢复 / 已关闭 |
| 行内「处置 / 复查」 | 进入告警详情 / 复查页 |

#### 3.3.6 告警详情 / 复查

顶部步骤条：**异常触发 → 确认处置 → 复查任务 → 恢复确认 → 关闭**。

| 操作 | 结果 |
|------|------|
| 「确认告警」 | 待确认 → 处理中 |
| 填「处置说明」→「保存处置记录」 | 追加处置轨迹（可多次记录） |
| 「创建复查任务」 | 自动生成一条「XX复查」任务（高优先级），告警转「待复查」，页面跳到调度工作台派单 |
| 复查任务正常完成后 | 告警转「已恢复」 |
| 「关闭告警」 | 已恢复 → 已关闭（需有有效复查恢复结果与关闭依据） |
| 「原始结果」/「来源任务」 | 分别跳到结果复核页 / 任务详情 |
| 复查任务提示区 | 「进入调度 / 执行监控 / 复查结果」 |

---

### 3.4 资源与地图

#### 3.4.1 设备资源树

| 操作 | 结果 |
|------|------|
| 左侧树 | 区域 → 设备；「全部设备」回到全集 |
| 右侧台账 | 搜索设备；设备名可点 → 设备巡检档案 |
| 「对象与检测项」 | 弹窗展示该设备的对象 / 检测项 / 点位；可跳「业务标注工作台」 |

#### 3.4.2 巡检点列表 / 巡检点详情

| 操作 | 结果 |
|------|------|
| 筛选 | 设备 / 地图 / 状态 + 关键词；「业务标注工作台」按钮 |
| 行内「编辑标注」 | 跳到点位标注工作台（定位到该地图） |
| 设备列 | 可点 → 设备巡检档案 |
| 点位详情页 | 显示点位信息、最近检测结果；「进入业务标注」 |

#### 3.4.3 点位标注与验证工作台

把地图上的"候选目标"变成可巡检的"业务点位"。

| 操作 | 结果 |
|------|------|
| 顶部地图选择 | 切换地图 |
| 左「候选目标与业务点位」 | 点选目标；「＋ 人工新增目标」补一个 |
| 中「点位标注画布」 | **双击地图**可新增目标点 |
| 右「业务身份与检测要求」 | 填写业务身份与检测要求 →「保存业务标注」（点位转"待验证 / 待重新验证"） |
| 「剔除误识别」 | 把候选目标标记为已剔除 |
| 「模拟自主试巡检验证」 | 选机器人后模拟验证，通过后点位转「已启用」并写入验证证据 |
| 顶部「同步与激活」/「发布检查 →」 | 分别跳地图同步页 / 地图详情页 |

#### 3.4.4 地图管理

5 个 Tab：

| Tab | 操作 |
|-----|------|
| 地图列表 | 搜索名称 / 区域；行内「详情」 |
| 地图详情 | 「下载版本清单」、「点位标注」、**「发布版本」**（弹「版本发布与影响检查」：**发布试验版本** 或 **正式发布并同步**）、地图预览点击进标注、**「查看机器人同步 →」** |
| 原始资料 | 「补充归档资料」（登记点云 / 视频）→「保存归档」 |
| 地图版本与机器人同步 | 「下发当前版本」；下发记录行内「模拟下一阶段回执 / 确认激活回执 / 重试 / 模拟失败」 |
| 地图点位变化管理 | 「登记变化」→ 弹「登记现场变化」→「登记」；行内「确认影响」（关联点位转"待重新验证"）、「提交修订」→ 弹「修订地图与点位版本」→「保存修订版本」、**「核对并关闭」**（需已发布且全部机器人同步完成） |

**＋ 导入地图**（右上角）：4 步向导 —— 来源与区域 → 地图文件 → 原始资料 → 校验与导入；可点「使用完整示例采集包」快速填充，最后「导入草稿」。

---

### 3.5 机器人管理

#### 3.5.1 机器台账

| 操作 | 结果 |
|------|------|
| 筛选 | 机器人关键词、区域、状态（空闲 / 执行中 / 充电 / 人工接管 / 故障 / 离线） |
| 视图切换 | 「运行视图 / 列表视图」 |
| 卡片页脚 | 「进入机器人运行监测 →」 |

#### 3.5.2 机器人运行监测

三个 Tab：**运行监测 / 任务 / 运行记录**。

| 区域 | 操作 |
|------|------|
| 运行监测 | 地图点选（机器人 / 点位）；「进入实时执行监控」看当前任务 |
| 任务 | 当前与历史任务；「管理机器人未来队列」→ 任务队列页；历史任务的「过程回放」→ 执行回溯内置页 |
| 运行记录 | 该机器人的事件时间轴 |

#### 3.5.3 能力与健康（`能力与健康`）

页头选机器人 → 查看载荷能力、移动能力、健康度、固件等（只读）。

#### 3.5.4 地图与版本（`地图与版本`）

查看该机器人使用的地图 / 点位版本是否与最新一致；「版本同步工作台」跳到地图同步页。

#### 3.5.5 人工操作与遥控

**接管是四步流程**（顶部步骤条）：申请接管 → 暂停确认 → 人工遥控 → 释放并恢复。

| 阶段 | 操作 | 结果 |
|------|------|------|
| 申请 | 「申请接管」 | 建立接管会话（状态"待暂停确认"） |
| 暂停确认 | 「确认机器人已暂停」 | 机器人转"人工接管"，任务转"暂停"，进入遥控 |
| 人工遥控 | 「前进 / 停止 / 后退 / 左移 / 右移」；「云台左转 / 云台右转 / 云台抬头 / 采集照片」 | 移动与云台动作生效；「采集照片」会生成一条**待复核结果**（不覆盖自主巡检结果） |
| 退出 | 「退出人工接管」→ 勾选（已确认版本 / 任务断点 / 结果一致）→「释放控制权」 | 会话"已释放"，机器人回到空闲或暂停 |
| 收尾 | 无会话且任务处于暂停时，「明确恢复任务」 | 任务回到「执行中」 |
| 审计 | 页内「遥控操作回执与审计」表 | 查看接管与遥控的全部指令记录 |

#### 3.5.6 机型专项

查看三类机型（机器狗 / 四轮车 / 挂轨）的差异：最低可派电量、是否参与充电规划、轨道区段、限速等（只读）。

---

### 3.6 分析与报表 › 指标中心

| 操作 | 结果 |
|------|------|
| 点 KPI 卡 | 展开「口径说明」（含公式、目标值、可下钻维度） |
| 口径面板「下钻查看明细」 | 跳到对应业务页（机器人 / 点位 / 任务 / 结果复核 / 告警 / 接管 / 计划） |
| 指标清单表末列「下钻」 | 同上 |
| 「口径配置」面板 | **仅管理员可见**：查看/调整成本与人力换算系数 |

**核心指标分组**：设备规模与在线率、任务与执行、结果与复核、告警与闭环、AI 效果（准确率 / 误报率）、工时与成本（管理员）。

---

### 3.7 系统设置（管理员）

| 页面 | 操作 |
|------|------|
| 角色 / 权限 | 查看角色权限矩阵（页面可见性、数据范围、按钮权限点） |
| 审计日志 | 搜索 + 按业务对象跳转；记录平台内的关键操作 |
| 上层任务服务 / 接口配置 | 输入业务编号 →「模拟提交业务任务」（幂等，重复编号不会重复建任务）；「统一调度」跳调度工作台；映射表内「查询任务」 |
| 接口日志 | 查看外部接口调用记录 |
| 字典 / 参数、能力 / 服务管理、组织 / 用户 | 配置项列表：＋ 新增 / 编辑 / 停用|启用 |
| 资源库 | 搜索；「归档详情」（跳地图存档）、「预览示意」、「下载版本清单」、「关联结果」 |

---

### 3.8 驾驶舱大屏（独立窗口）

顶栏右上角「驾驶舱大屏」→ 新标签页打开，顶部 Tab 切换两个驾驶舱；「展示全屏 / 退出大屏」可切换全屏。

**管理驾驶舱**（综合总览 / 运营管理，默认视图随角色）：
- 厂区筛选、口径说明展开收起；
- 「巡检任务情况」时间分段：今日 / 近 7 天 / 近 30 天 / 全部；
- 「告警趋势」近 7 天 / 近 30 天；
- 「核心指标（含环比）」点任一指标 → 下钻弹窗。

**调度指挥驾驶舱**（值守 + 指挥）：
- 左栏设备池（厂区 / 机型 / 状态三重筛选，点选设备）；
- 中栏地图（点位图层 / 充电桩 / 轨道区段 / BIM 视图图层切换）；
- 当前设备卡：「进入操控台 / 申请接管」「一键返航」；
- 机器人导航：方向控制（需先建立接管会话）；任务导航：「暂停任务 / 继续任务」；
- 云台控制与「采集照片」；
- 右栏：告警合并列表、SLA 倒计时列表、双视频窗；
- 高风险操作：「紧急停止」（弹「确认紧急停止」→「确认急停」：暂停全部执行中任务）。

---

## 四、核心业务流程（端到端操作剧本）

### 流程 A：地图与点位，从导入到可派单

> **目标**：让一个区域具备"可以被机器人巡检"的资格。

| 步 | 操作位置 | 做什么 |
|----|---------|--------|
| 1 | 资源与地图 › 地图管理 | 右上「＋ 导入地图」，走 4 步向导，最后「导入草稿」 |
| 2 | 资源与地图 › 地图管理 › 原始资料 | 「补充归档资料」登记点云 / 视频 →「保存归档」 |
| 3 | 资源与地图 › 点位标注与验证工作台 | 选地图 → 选/新增候选目标 → 填业务身份与检测要求 →「保存业务标注」 |
| 4 | 同上 | 选机器人 →「模拟自主试巡检验证」→ 点位转「已启用」 |
| 5 | 资源与地图 › 地图管理 › 地图详情 | 「发布版本」→「正式发布并同步」 |
| 6 | 地图版本与机器人同步 | 「下发当前版本」→ 逐条「模拟下一阶段回执」直到「已同步」 |

**验收标志**：地图「已发布」，点位「已启用」，机器人侧版本与地图一致（`mX / pX` 相同），调度时不再因版本不一致被拒单。

---

### 流程 B：计划 → 任务 → 调度 → 执行 → 结果

| 步 | 操作位置 | 做什么 |
|----|---------|--------|
| 1 | 巡检执行 › 巡检模板 | ＋ 新建模板（选点位、优先级、完成时限）→「保存模板」 |
| 2 | 巡检执行 › 计划列表 | ＋ 新建计划（选模板 + 周期）→「保存计划版本」 |
| 3 | 计划列表 | 行内「模拟自动下发计划任务」→ 生成待调度任务并跳到任务列表 |
| 4 | 巡检执行 › 任务调度工作台 | 左选任务、右选机器人 → 选「立即执行」/「加入队列」→「立即派单执行」/「派单入列」 |
| 5 | 巡检执行 › 实时执行监控 | 看阶段推进（7 段）与实时结果；需要时可「演示事件注入」推进 |
| 6 | 结果与异常 › 巡检结果查询 | 按任务分组查结果，展开看检测项明细 |
| 7 | 结果与异常 › 执行回溯记录 | 回看这次任务的执行事件与采集证据 |

**任务状态变化**：待调度 → 已分配 → 下发中 → 待执行 → 执行中 → 完成 / 部分完成 / 失败。

---

### 流程 C：结果复核

| 场景 | 操作 | 结果 |
|------|------|------|
| 确认无误 | 复核结论选「确认」→ 填依据 →「提交复核」 | 结果转「已确认」，任务有效项不变 |
| 数值识别错了 | 结论选「修正」→ 填正确最终值 → 选最终业务状态 → 填依据 →「提交复核」 | 最终值更新，保留原始值与识别值，追加审计记录 |
| 这条结果不该算 | 结论选「标记无效」→ 填无效原因 →「提交复核」 | 该项从任务有效项剔除，任务状态**重新计算**（可能由"完成"变"部分完成"） |

---

### 流程 D：异常告警闭环

| 步 | 操作 | 结果 |
|----|------|------|
| 1 | 结果中检出异常（或执行监控注入异常） | 生成告警，状态「待确认」；执行监控同时产生异常结果 |
| 2 | 告警事件 → 行内「处置 / 复查」 | 进入告警详情 |
| 3 | 「确认告警」 | 转「处理中」 |
| 4 | 填处置说明 →「保存处置记录」 | 处置轨迹追加 |
| 5 | 「创建复查任务」 | 生成「XX复查」任务并跳调度工作台 |
| 6 | 调度工作台派单 → 执行监控推进 | 复查任务执行 |
| 7 | 复查任务产出**正常**结果 | 告警自动转「已恢复」 |
| 8 | 回告警详情 →「关闭告警」 | 转「已关闭」，闭环完成 |

> 若判断是误报，可直接走误报标记路径（关闭告警并写入 AI 反馈队列，供误报率统计消费）。

---

### 流程 E：现场发生变化，怎么改地图

| 步 | 操作 | 结果 |
|----|------|------|
| 1 | 地图管理 › 地图点位变化管理 →「登记变化」 | 新增一条变化记录（待确认） |
| 2 | 行内「确认影响」 | 变化转「维护中」，关联点位转「待重新验证」 |
| 3 | 行内「提交修订」→「保存修订版本」 | 地图版本 +1、点位集合 +1，地图回「草稿」 |
| 4 | 地图详情「发布版本」 | 重新发布 |
| 5 | 「核对并关闭」 | 变化转「已完成」（前置条件：版本已发布且全部机器人同步完成） |

---

### 流程 F：人工接管与遥控

见 3.5.5。要点：**接管必须先申请、机器人必须先确认暂停**；退出时需核对版本 / 任务断点 / 结果一致性才允许释放；全过程写入遥控审计表。

---

### 流程 G：临时任务 / 紧急插队

| 场景 | 操作 |
|------|------|
| 临时加一次巡检 | 任务列表「＋ 临时巡检任务」→「进入调度」→ 派单；或在实时执行监控「＋ 下发临时任务」直接下发 |
| 紧急任务要插队 | 任务队列页选中排队任务 →「任务抢占」→「模拟暂停确认并抢占」（当前执行任务被暂停并保留断点） |
| 要改派给别人 | 任务队列页「改派」→「确认撤回并改派」 |

---

### 流程 H：上层业务系统接入

| 步 | 操作 |
|----|------|
| 1 | 系统设置 › 上层任务服务 / 接口配置：输入业务编号 →「模拟提交业务任务」 |
| 2 | 系统**按业务编号幂等**生成任务（同编号不会重复建） |
| 3 | 「统一调度」→ 调度工作台派单 |
| 4 | 映射表内「查询任务」可查该业务编号对应的任务 |

---

### 流程 I：大屏值守（调度指挥）

1. 顶栏「驾驶舱大屏」→ 新标签页 → 切到「调度指挥驾驶舱」；
2. 左栏按厂区/机型/状态筛出设备，点选目标机器人；
3. 需要人工介入：切到「人工操作与遥控」页完成接管（四步）；
4. 出现紧急情况：右栏告警列表定位异常 → 必要时「紧急停止」；
5. 关注右栏 **SLA 倒计时**列表：超时会自动预警与升级（工单域）。

---

## 五、角色与权限

### 5.1 角色差异

| 维度 | 管理员（王姐） | 非管理员（老周） |
|------|---------------|----------------|
| 可见中心 | 7 个（含系统设置） | 6 个（无系统设置） |
| 数据范围 | 全租户 | 分管厂区 |
| 驾驶舱默认视图 | 综合总览 | 运营管理 |
| 审批 | 接管审批、导出审批、配置发布、插单审批、资质审批 | 无 |
| 独有权限 | 配置编辑/发布、组织编辑、权限编辑、审计查看、导出审批、口径管理 | — |

### 5.2 权限点（按业务域）

| 业务域 | 权限点 |
|--------|--------|
| 告警与工单 | 告警确认、一键派单、超时升级、工单派单、工单转派、工单处置、验收通过、验收驳回、批量验收、误报标记 |
| AI / 复核 | AI 复核、标注反馈 |
| 计划与调度 | 计划编辑、调度下发、紧急插队、临时任务 |
| 机器人控制 | 远程接管、紧急停止、一键返航充电、接管申请、接管审批、双人复核 |
| 现场保障 | 现场保障、现场派单 |
| 报表与驾驶舱 | 报表查看、导出申请、导出审批、口径管理、指标查看、驾驶舱查看/下钻/订阅、大屏模式、驾驶舱指挥 |
| 系统管理（仅管理员） | 配置编辑、配置发布、组织编辑、权限编辑、审计查看 |

### 5.3 界面上的体现（当前演示版已接门控的位置）

| 位置 | 权限点 |
|------|--------|
| 指标中心 › 口径配置面板 | 口径管理（仅管理员可见） |
| 调度指挥驾驶舱 › 紧急停止 | 紧急停止 |
| 调度指挥驾驶舱 › 进入操控台 / 申请接管 / 采集照片 | 远程接管 |
| 调度指挥驾驶舱 › 一键返航 | 一键返航充电 |
| 调度指挥驾驶舱 › 暂停 / 继续任务 | 调度下发 |
| 人工操作与遥控 › 申请接管 | 接管申请 |

> 其余操作按钮当前未做按钮级门控，仅通过"页面可见性"控制（例如非管理员看不到整个系统设置中心）。

---

## 六、状态与规则速查（验收对照）

### 6.1 任务状态

| 状态 | 含义 | 可以走到 |
|------|------|---------|
| 待调度 | 已生成、未指定机器人 | 已分配 / 待执行 / 取消 |
| 已分配 | 已选定机器人、未下发 | 下发中 / 待执行 / 待调度（撤回） |
| 下发中 | 指令已发、等机器人回执 | 待执行 / 待调度 |
| 待执行 | 机器人已接收、排队中 | 执行中 / 待调度（撤回） |
| 执行中 | 自主巡检中 | 暂停 / 完成 / 部分完成 / 失败 |
| 暂停 | 中断但保留断点 | 执行中 |
| 完成 | 全部检测项有效 | （复核判无效可能回退为部分完成/失败） |
| 部分完成 | 部分检测项有效 | — |
| 失败 | 无有效检测项 | — |
| 取消 | 主动终止且无已完成项 | — |
| 超期 | 预留状态（当前演示数据未产生） | — |

**主链路**：待调度 → 已分配 → 下发中 → 待执行 → 执行中 → 完成 / 部分完成 / 失败。

### 6.2 执行阶段（7 段）

前往目标区域 → 搜索目标 → 目标锁定 → 自主调整 → 数据采集 → 本地处理 / AI 分析 → 结果生成。

### 6.3 告警与工单

| 对象 | 状态流转 |
|------|---------|
| 告警 | 待确认 →（确认/派单）→ 处理中 →（创建复查任务）→ 待复查 →（复查正常）→ 已恢复 →（关闭）→ 已关闭；任意状态可因误报直接关闭 |
| 工单 | 待派单 → 已派单 → 已接单 → 处置中 → 待验收 → 已验收 → 已关闭 |
| SLA | 正常 → 预警 → 已升级（超时自动推进；执行人 ≠ 验收人） |

### 6.4 资源与地图状态

| 对象 | 状态 |
|------|------|
| 地图 | 草稿 → 试验发布 → 已发布 |
| 点位 | 待标注 → 待验证 → 已启用（另有 待重新验证 / 停用） |
| 版本同步 | 传输中 → 校验中 → 待激活 → 已同步（可失败并可重试） |
| 现场变化 | 待确认 → 维护中 → 待同步 → 已完成（可驳回） |
| 接管会话 | 待暂停确认 → 已接管 → 退出核对中 → 已释放（可超时） |
| 机器人 | 空闲 / 执行中 / 暂停 / 人工接管 / 离线 / 充电 / 故障 / 维护 |

### 6.5 调度校验规则（派单被拒的原因清单）

区域匹配 · 检测能力（可见光 / 红外 / 气体 / 声音）· 原子动作能力 · 移动能力（爬楼 / 越障 / 跨楼层 / 道路行驶 / 固定轨道等）· 电量不低于机型门槛（机器狗 40% / 四轮车 25% / 挂轨 15%）· 轨道区段未被占用 · 机器人状态可用 · 无人工接管会话 · 设备未停用 · 点位已启用 · 地图与点位版本与任务快照一致 · 机器人无冲突任务。

### 6.6 关键参数（可在配置中心调整）

| 参数 | 默认值 | 用途 |
|------|--------|------|
| 即将超期阈值 | 15 分钟 | 工作台与指标中心的"即将超期"口径 |
| 成本节省系数 | 0.12 万元 / 完成任务的换算 | 成本节省指标 |
| 人力节省系数 | 0.5 人·班 / 完成任务的换算 | 人力节省指标 |

**指标口径唯一**：全平台 26 项指标只在指标中心定义，工作台 / 驾驶舱 / 报表都取同一口径，不允许页面自行计算。

### 6.7 常见失败提示与处理（操作排错）

演示环境里"弹窗报错"绝大多数是**业务校验在生效**，不是程序异常。对照处理：

| 界面提示 / 现象 | 原因 | 处理建议 |
|------|------|---------|
| 派单被拒：区域不匹配 / 能力不符 / 电量低于门槛 / 版本不一致 / 已有任务… | 调度校验未通过（见 6.5） | 换满足条件的机器人，或先「下发当前版本」同步地图、先充电 |
| 告警无法关闭 | 尚无有效复查恢复结果 | 先「创建复查任务」并执行出正常结果，再关闭（见流程 D） |
| 任务状态没变 | 复核选「确认」不改变有效项 | 正常；只有「修正 / 标记无效」会触发任务状态重算（见 3.3.2） |
| 重置后数据没变 | 离线（`file://`）模式本机存储被禁用 | 用开发服务或正常 http 访问（见 7.1） |
| 一切换角色菜单变少 | 非管理员看不到系统设置 | 用管理员角色查看（见 1.4 / 5.1） |
| 地图改完点位不生效 | 未发布且未同步到机器人 | 地图详情「发布版本」→ 同步页「下发」→ 逐条「确认激活回执」 |
| 弹窗提交无反应 | 必填项未填（如复核依据 / 无效原因） | 补齐必填项再提交 |

---

## 七、数据、存储与交付

### 7.1 数据存在哪、怎么重置

- 数据保存在**浏览器本机存储**；同一浏览器同一地址刷新后保留。
- 顶栏「重置演示数据」→「确认重置」：恢复到初始演示数据并回到默认落地页。
- 换浏览器 / 换电脑 / 清缓存 / 无痕窗口：都会是初始演示数据。
- 离线（`file://`）打开时，部分浏览器禁用本地存储，此时页面可用但刷新会回到初始数据（已做容错）。

### 7.2 演示数据覆盖范围

平台预置了完整的一套演示数据，足以跑通全部流程：

| 分类 | 内容 |
|------|------|
| 资源 | 地图（含候选目标与版本历史）、巡检点位、充电桩、轨道区段、备件 |
| 机器人 | 三类机型（机器狗 / 四轮车 / 挂轨）的机器人，含能力、电量、健康度、固件、地图版本 |
| 作业 | 巡检模板、计划、任务（待调度 / 执行中 / 已完成等各状态）、执行日志与采集证据图 |
| 结果与异常 | 巡检结果（含正常 / 异常、待复核 / 已确认 / 已识别）、告警（含各状态）、AI 反馈 |
| 处置 | 工单（含 SLA 与责任链）、验收、接管申请、现场保障工单 |
| 其他 | 排班（与资质联动）、通知、审计与接口日志、配置项 |

### 7.3 离线单文件版（推荐的分发方式）

| 步骤 | 命令 / 操作 |
|------|------------|
| 1 | `npx vite build` 或直接执行下一步（导出脚本会自行构建） |
| 2 | `node scripts/export-static.mjs` |
| 3 | 产物：`静态版/index.html`（JS / CSS / 图标 / 图片**全部内联**，无任何外链） |
| 4 | 分发：压缩整个 `静态版` 文件夹后发送（直接发 `.html` 附件常被 IM / 邮箱拦截） |
| 5 | 使用：客户双击 `index.html` 即可打开，无需联网、无需安装 |

### 7.4 已知限制（交付时请提前说明）

| 项 | 说明 |
|----|------|
| 无后端 | 所有数据在本机；多人协同、真实数据接入需在实施阶段对接 |
| 无真实控制 | 不连接真实机器人、不发真实指令 |
| 部分页面为占位 | 巡检标准、告警规则、路线管理、组织 / 用户、能力 / 服务管理、基础分析、资源库等已隐藏入口或为占位页（见附录 9.2） |
| 生产构建 | `npm run build` 含类型检查，当前存在若干历史遗留类型告警；`npx vite build` 与 `npm run dev` 均正常 |
| 语言 | 界面为简体中文，面向国内现场 |

---

## 八、操作日志与全链路追溯（平台的"操作日志"在哪）

> 你最初问的"操作日志"，在平台里不是某一个页面，而是一套**贯穿全流程的留痕**：谁、在什么时候、对什么、做了什么、依据是什么。不同性质的记录放在不同位置，下面按用途给出入口与用法。

### 8.1 六类日志分别在哪

| 日志 | 入口 | 记录内容 | 用途 |
|------|------|---------|------|
| 执行回溯（过程回放） | 结果与异常 › 执行回溯记录；设备档案「巡检任务」Tab 的「过程回放」；机器人运行监测「任务」Tab 历史任务「过程回放」；结果详情页「任务过程回放 →」 | 单个任务从下发 → 开始 → 逐项采集回执（**真实读数 + 现场图**）→ 结束 / 失败原因 的完整事件时间轴 | 查"这次巡检到底采了什么、读数多少、谁采的" |
| 复核审计 | 结果详情 / 复核页提交后写入；指标中心、执行回溯可下钻查看 | 每条结果的人工复核结论（确认 / 修正 / 标记无效）、最终值、依据、操作人、时间 | 复核责任追溯 |
| 处置轨迹 | 告警详情 / 复查页「保存处置记录」 | 每次告警确认、处置说明、复查任务、关闭依据 | 异常闭环责任追溯 |
| 审计日志 | 系统设置 › 审计日志（**管理员**） | 平台关键操作的统一流水（业务对象 + 动作 + 时间），可搜索、可按对象跳转 | 全平台操作合规审计 |
| 接口日志 | 系统设置 › 接口日志（**管理员**） | 外部系统调用记录（如上层业务系统提交任务） | 集成问题排查 |
| 调度记录 | 任务调度工作台「任务分配与接收记录」、机器人任务队列 | 派单 / 入队 / 改派 / 抢占 / 插队决策与机器人回执 | 调度过程追溯 |
| 遥控审计 | 人工操作与遥控页「遥控操作回执与审计」 | 接管会话、方向 / 云台 / 采集指令回执 | 遥控操作合规追溯 |

### 8.2 执行回溯（过程回放）详解

**日常最常用的一条日志线**，回答"某次巡检究竟发生了什么"。

| 维度 | 说明 |
|------|------|
| 进入方式 | ① 执行回溯记录页按任务选；② 设备巡检档案「巡检任务」Tab 中已结束任务「过程回放」；③ 机器人运行监测「任务」Tab 历史任务「过程回放」；④ 结果详情页「任务过程回放 →」 |
| 左侧列表 | 任务列表，支持搜索、按状态筛选、时间范围（近 7 天 / 近 30 天 / 全部 / 自定义起止） |
| 右列时间轴 | 每个采集回执都带：阶段、点位、检测项、原始值 → 识别值、现场照片；失败项标注失败原因 |
| 只读 | 执行回溯为只读回放，不在此页做写操作 |

### 8.3 审计日志与接口日志详解

| 操作 | 结果 |
|------|------|
| 审计日志搜索框 | 按关键字过滤操作流水 |
| 业务对象链接 | 点对象编号跳到对应业务页（任务 / 结果 / 告警等） |
| 接口日志 | 查看外部调用；如上层业务系统「模拟提交业务任务」会在此留痕 |
| 只读 | 审计 / 接口日志为合规记录，演示环境不可修改 |

### 8.4 操作留痕速查（哪些动作会写日志）

| 动作 | 写入的日志 |
|------|-----------|
| 提交复核 | 复核审计 + 审计日志 |
| 确认 / 处置 / 复查 / 关闭告警 | 处置轨迹 + 审计日志 |
| 派单 / 入队 / 改派 / 抢占 | 调度记录 + 审计日志 |
| 申请接管 / 遥控指令 / 释放 | 遥控审计 + 审计日志 |
| 外部系统提交任务 | 接口日志 + 审计日志 + 生成任务 |
| 地图发布 / 同步 / 修订 | 审计日志（地图版本变更） |

---

## 九、附录

### 9.1 页面地图（演示时可直接改地址栏跳转）

| 一级中心 | 页面 | 路由 | 主要操作 |
|---------|------|------|---------|
| 工作台 | 我的工作台 | `#/workbench` | 点 KPI 看明细弹窗、去复核 |
| 巡检执行 | 巡检模板 | `#/resources/templates` | 新建 / 编辑 / 停用 / 删除模板 |
| 巡检执行 | 计划列表 | `#/planning/plans` | 新建 / 编辑计划、模拟下发生成任务 |
| 巡检执行 | 计划编辑 | `#/planning/plan-edit` | 编辑 9 项计划参数 |
| 巡检执行 | 任务列表 | `#/planning/tasks` | 筛选、建临时任务、看详情与结果 |
| 巡检执行 | 任务调度工作台 | `#/dispatch/workbench` | 选任务 + 选机器人 → 派单 / 入队 |
| 巡检执行 | 机器人任务队列 | `#/dispatch/queue` | 排序 / 暂停 / 改派 / 抢占 / 出列 |
| 巡检执行 | 实时执行监控 | `#/execution/live` | 任务控制、临时任务、演示事件注入 |
| 巡检执行 | 执行回溯记录 | `#/execution/replay` | 按任务回看执行事件与证据 |
| 结果与异常 | 巡检结果查询 | `#/results/query` | 按任务分组查结果、证据 / 复核 |
| 结果与异常 | 结果详情 / 复核 | `#/results/review/:id` | 提交人工复核 |
| 结果与异常 | 设备巡检档案 | `#/results/equipment` | 5 个 Tab 按设备看全历史 |
| 结果与异常 | 告警事件 | `#/alarms/events` | 状态筛选、进入处置 / 复查 |
| 结果与异常 | 告警详情 / 复查 | `#/alarms/detail/:id` | 确认 / 处置记录 / 创建复查 / 关闭 |
| 资源与地图 | 设备资源树 | `#/resources/equipment` | 设备台账、对象与检测项弹窗 |
| 资源与地图 | 巡检点列表 | `#/resources/points` | 筛选、编辑标注 |
| 资源与地图 | 点位标注与验证工作台 | `#/resources/annotation` | 标注 / 剔除 / 试巡检验证 |
| 资源与地图 | 地图管理 | `#/robots/maps` | 导入、发布、同步、变化管理 |
| 机器人管理 | 机器台账 | `#/robots/monitor` | 筛选机器人、进入运行监测 |
| 机器人管理 | 机器人运行监测 | `#/robots/detail/:id` | 地图 / 任务 / 运行记录 |
| 机器人管理 | 能力与健康 | `#/robots/health` | 能力、健康度、固件 |
| 机器人管理 | 地图与版本 | `#/robots/map` | 查看版本一致性、进同步工作台 |
| 机器人管理 | 人工操作与遥控 | `#/robots/manual` | 接管四步 + 遥控 + 审计 |
| 机器人管理 | 机型专项 | `#/robots/device` | 机型差异配置 |
| 分析与报表 | 指标中心 | `#/analytics/metrics` | 口径说明、下钻明细 |
| 系统设置 | 角色 / 权限 | `#/system/roles` | 权限矩阵 |
| 系统设置 | 审计日志 | `#/system/audit` | 搜索、按对象跳转 |
| 系统设置 | 上层任务服务 / 接口配置 | `#/integration/services` | 模拟提交业务任务、统一调度 |
| 系统设置 | 接口日志 | `#/integration/logs` | 接口调用记录 |
| 系统设置 | 字典 / 参数 等 | `#/system/settings` | 配置项新增 / 编辑 / 停用 |
| 独立入口 | 驾驶舱大屏 | `#/screen/bigscreen` | 管理驾驶舱 / 调度指挥驾驶舱 |

**内置只读页（无侧边栏入口，由业务页进入）**

| 页面 | 路由 | 进入方式 |
|------|------|---------|
| 结果详情 | `#/results/equipment/detail/:id` | 设备巡检档案的结果编号 |
| 告警详情 | `#/results/equipment/alarm/:id` | 设备巡检档案的关联告警 |
| 执行回溯 | `#/results/equipment/replay/:id` | 设备档案「过程回放」、机器人历史任务 |
| 巡检点详情 | `#/resources/points/detail/:id` | 巡检点列表 / 地图点选 |
| 任务详情 | `#/planning/tasks/detail/:id` | 任务列表 / 各处的任务引用 |

### 9.2 隐藏页 / 占位页（客户看不到入口）

| 页面 | 状态 |
|------|------|
| 巡检标准、告警规则、路线管理（含"轨道"）、能力 / 服务管理、基础分析、资源库、组织 / 用户 | 占位或未完成，**已从侧边栏隐藏**，路由保留 |
| 任务日历、调度记录、目标与要求 | 功能可用但当前未在菜单暴露 |

### 9.3 常见问题（FAQ）

| 问题 | 答案 |
|------|------|
| 点"派单"失败，提示不满足条件？ | 这是设计好的调度校验（区域 / 能力 / 电量 / 版本 / 状态等），按提示换机器人或先同步版本 |
| 结果里"识别值"和"最终值"为什么不同？ | 识别值是 AI 输出，最终值是复核后的业务值；不修正时两者相同 |
| 复核后任务状态变了？ | 判「标记无效」会把该项从有效项剔除，任务状态会重新计算 |
| 告警怎么才能关闭？ | 需先有复查任务的**正常**结果（转"已恢复"），再用有效依据关闭 |
| 机器人为什么派不出去？ | 可能是电量低于机型门槛、地图/点位版本落后、离线或正被人工接管 |
| 地图改了为什么还要"同步"？ | 机器人按本地版本作业，必须下发并激活新版本才会生效 |
| 临时任务和计划任务有什么区别？ | 计划任务由模板 + 计划定时生成；临时任务手工创建或直接从执行监控下发 |
| 换电脑后数据不见了？ | 数据只在本机浏览器；演示前请在同一台机器上准备好 |
| 误操作了怎么恢复？ | 顶栏「重置演示数据」→「确认重置」 |

### 9.4 演示前检查清单

- [ ] 打开平台，确认默认进入「我的工作台」
- [ ] 确认 KPI 有数据（待复核 / 待确认告警 / 待调度 / 即将超期 / 执行中至少各有 1 条）
- [ ] 选好演示角色（管理员看全功能，非管理员演示权限差异）
- [ ] 提前走一遍第四章的流程 B 与流程 D（这两条最能体现平台价值）
- [ ] 需要大屏时，提前打开「驾驶舱大屏」并调好分辨率
- [ ] 演示结束前不要执行「重置演示数据」

### 9.5 常见页面跳转关系（操作动线）

| 从页面 | 常去页面 | 跳转方式 |
|--------|---------|---------|
| 我的工作台 | 结果查询 / 告警事件 / 调度工作台 / 机器人监测 / 任务列表 / 实时执行监控 | KPI 弹窗底部「进入 XX」；待复核条目「去复核」→ 结果复核 |
| 巡检结果查询 | 结果详情 / 复核、执行回溯 | 行内「证据 / 复核」、任务「过程回放」 |
| 告警事件 / 告警详情 | 结果复核、任务详情、调度工作台 | 行内「处置 / 复查」、提示区「进入调度」 |
| 任务调度工作台 | 机器人任务队列、实时执行监控 | 「任务分配与接收记录」「时间轴摘要」内跳转 |
| 实时执行监控 | 人工操作与遥控 | 「人工接管 →」 |
| 设备巡检档案 | 结果详情 / 告警详情 / 执行回溯（内置只读页） | 编号链接、Tab 内「过程回放」 |
| 地图详情 | 地图版本与机器人同步 | 「查看机器人同步 →」 |
| 计划列表 | 任务列表 | 「模拟自动下发计划任务」 |
| 指标中心 / 驾驶舱 | 对应业务页（机器人 / 点位 / 任务 / 结果 / 告警 / 接管 / 计划） | KPI / 指标「下钻」 |

> 内置页（结果详情 / 告警详情 / 执行回溯）的面包屑、返回、侧边栏高亮都挂在原模块下，不会让使用者"迷路"。

---

## 附：文档索引（本目录其余文档）

| 文档 | 用途 |
|------|------|
| `平台操作手册.md`（本文） | 面向客户 / 使用者的操作手册与业务流程说明 |
| `README.md` | 项目说明、技术栈、启动与离线导出、关键约定、已知问题 |
| `PAGE_MAP.md` | 页面 ↔ 文件 ↔ 路由 ↔ 数据来源速查（研发用） |
| `巡检业务平台原型交互评审-01.md` | 首轮交互评审记录与落地回访核对 |
| `页面优化清单-01.md` | 逐页 UI/UX 优化清单与优先级 |
