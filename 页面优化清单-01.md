# 智能机器巡检平台原型 · 页面 UI/UX 优化清单-01

| 项目 | 内容 |
|------|------|
| 文档名称 | 页面优化清单-01 |
| 项目路径 | `d:\traeFile\AI对话\9_23\巡检业务平台原型` |
| 技术栈 | React 19 + TypeScript + Vite（`src/` 下 32 个 `.tsx` + 9 个 `.ts` + 1 个 `styles.css`） |
| 产出日期 | 2026-09-26 |
| 用途 | 供后续开发按优先级逐条落地；每条给「问题 → 位置 → 建议改法」 |
| 优先级口径 | **P0** 影响可用性/产生误导，先做；**P1** 信息层级与一致性；**P2** 视觉打磨 |

> 行号基于 2026-09-26 版本，代码改动后会漂移，请以 `文件 + 符号名` 为准搜索。

---

## 一、全局共性问题（先统一处理，收益最大）

| 编号 | 问题 | 位置 | 建议改法 | 优先级 |
|------|------|------|----------|--------|
| G1 | **徽标颜色靠"文案正则"决定**：「达标 / 未达标 / 观察」不命中规则 → 全部渲染成默认灰，好坏无法区分（指标中心"达标"列尤其明显） | `UI.tsx:Badge` | 给 `Badge` 增加显式 `tone`（ok/warn/danger/info/neutral）入参；颜色由语义决定，不再猜文案 | **P0** |
| G2 | **"假可点击"**：多处 `<button>` 无 `onClick` 或 `onClick={() => undefined}`，却有 hover 反馈 | `MonitorCockpit.tsx` 设备行、`DispatchCockpit.tsx` BIM 按钮、`MonitorCockpit.tsx` 观察项 | 无行为的改为 `<div>`；确需预留的加禁用态 + "待接入"提示 | **P0** |
| G3 | **内部编码/术语外露**：`m1 / p2`、`原子动作`、`CONTROL_ACK`、`TASK_FINISHED`、`targetId`、`TASK_PAGE_SIZE` 语义的"完成度" | `Planning.tsx`、`Scheduling.tsx`、`Execution.tsx`、`ExecutionWorkbench.tsx` | 统一走中文映射字典；原始编码放 `title` 提示 | **P0** |
| G4 | **术语一义多词**：无目标值 / 观察项 / 观察；前进后退 vs 左移右移；口径说明 / 指标口径说明 | `Metrics.tsx`、`MonitorCockpit.tsx`、`ControlPad.tsx` | 建立「术语表」并全站替换（建议放 `src/data/glossary.ts`） | P1 |
| G5 | **危险操作无二次确认**：终止任务、跳过检测项、释放控制权均可直接触发；而模板删除又用原生 `window.confirm`，风格不一 | `ExecutionWorkbench.tsx:285-298`、`Execution.tsx:119-136`、`Planning.tsx:112` | 统一走应用内 `Modal` 二次确认；未满足前置条件时按钮禁用并给出原因 | **P0** |
| G6 | **反馈信息太技术**：全局 toast 统一是「操作成功 · 对象ID」 | `store.tsx:act` | 改为业务语义（如「已将 罐区午前例行巡检 派给 1号机器人」） | P1 |
| G7 | **空态缺失或自相矛盾**：模板为空时因 `s.templates[0]` 直接白屏；执行监控无在跑任务却仍渲染历史任务地图/视频，同时又显示"没有执行中任务" | `Planning.tsx:10,13`、`ExecutionWorkbench.tsx:39-43 vs 241-248` | 补齐空态组件；无数据时统一只显示空态，不渲染主区 | **P0** |
| G8 | **同一功能两套 UI / 双入口**：计划编辑（弹窗 vs 独立页）、调度决策块（dispatch vs queue）、机器人台账（OperationsLegacy vs Operations）、结果详情（review vs result-detail）、设备资源（equipment vs archive） | 见各页 | 每类保留唯一入口，删除历史实现；确需两处展示时明确职责边界 | P1 |
| G9 | **同口径数字重复铺陈**：驾驶舱 KPI 条与下方卡片重复；指标中心卡片/口径表/清单表三处重复 | `MonitorCockpit.tsx:165-206 vs 434-504`、`Metrics.tsx:46-137` | 一处详列 + 其余只做摘要或下钻 | P1 |
| G10 | **表格能力弱**：仅"上一页/下一页"，无页码、无每页条数、无排序 | `Business.tsx:Pager`、`UI.tsx:Table` | `Pager` 增加页码与每页条数；`Table` 增加可选 `sortKeys` | P1 |
| G11 | **图表可读性弱**：趋势图无纵轴刻度/单位/阈值线；X 轴逐点打结果 ID 必然重叠；空数据仍画坐标系 | `ResultTrend.tsx:13-16,42`、`MonitorCockpit.tsx:395` | 加纵轴刻度与单位、目标/阈值线、异常点改"形状+颜色"、空态不画轴 | P1 |
| G12 | **权限组件只会"隐藏"**：`<Can>` 无权限直接 `return null`，页面高度跳动，用户也不知道"因权限少了什么" | `Can.tsx:21-22` | 支持 `fallback`（禁用 + 原因 / 占位），或页面级汇总"当前角色不可见的 N 项" | P1 |
| G13 | **解释性文案位置错**：业务规则塞进列头、说明放在表格下方（看表时看不到） | `Planning.tsx:260-261,610-612`、`Scheduling.tsx:126-128` | 列头改精简 + `title` 提示；说明上移到表头 hint 或就近折叠区 | P2 |
| G14 | **内置页缺"进入方式"提示**：结果详情 / 告警详情 / 执行回溯为隐藏内置页，只在个别入口可点 | `navigation.ts` routeMeta、各页 `ObjectLink` | 关键对象的名字/编号统一可点（用 `ObjectLink` 的 `to`），并在空态/表格中说明可下钻 | P1 |

---

## 二、工作台 `workbench`（`pages/Workbench.tsx`）

用途：默认首页，6 个待办 KPI 作为入口，点击后在弹窗内展开该类明细（页面不再平铺长列表）。

| 编号 | 优化点 | 位置 | 建议 | 状态 |
|------|--------|------|------|------|
| W1 | ~~「原地处理」能力丢失（复核弹窗为死代码）~~ | `Workbench.tsx` | 死代码已删除；工作台统一只读展示，待复核条目改为「去复核」跳转结果复核页 | ✅ 已修复 |
| W2 | 待办数据交叉重复：同一条任务同时出现在「待调度任务」与「即将超期任务」 | `Workbench.tsx:62,85-90` | 二者口径互补（"即将超期"排除"待调度"）；注意需同步改指标 `dueSoonTasks`，否则 KPI 与明细数字不一致 | P1 |
| W3 | ~~KPI 6 项 vs 面板 5 块，数量不对齐~~ | `Workbench.tsx` | 现为 6 个 KPI ↔ 6 个明细弹窗（含「执行中任务」），一一对应 | ✅ 已解决 |
| W4 | ~~同行面板高度参差~~ | `styles.css` | 改为弹窗展示后不再存在网格对齐问题，相关看板样式已清理 | ✅ 已不适用 |
| W5 | 待办条目的"来源任务 / 点位"是死链（`ObjectLink` 默认不下钻） | `Workbench.tsx` | 接入 `to="task-detail" / "point"`，让用户能直接跳详情 | P1 |
| W6 | 明细弹窗内条目不按紧急度排序 | `Workbench.tsx` | 按「异常 > 临近超期 > 普通」排序，或加"紧急"分组 | P1 |
| W7 | 明细弹窗内逐条操作入口不全 | `Workbench.tsx` | 待复核已有逐条「去复核」；告警 / 待调度条目也可补「去处置 / 去派单」按钮（跳对应页面） | P2 |

---

## 三、巡检执行

### 3.1 计划列表 `plans`（`pages/Planning.tsx`）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| PL1 | 列头里塞整句说明（"模板（注：通过模板，实现计划/任务复用）"），表头被撑高且与单元格重复 | `Planning.tsx:260-261` | 列头精简为「模板」「周期 / 时间」，说明改 `title` 提示 |
| PL2 | 行内主操作叫「模拟自动下发计划任务」，"模拟"暴露原型语义 | `Planning.tsx:290` | 改「立即生成任务」；成功后提示"已生成 N 条任务"而不是只跳页 |
| PL3 | 缺关键列：该计划**已生成任务数 / 下次执行时间** | `Planning.tsx:271-273` | 增列，避免为了解计划在跑与否而跳页 |
| PL4 | 操作能力不一致：模板有启用/停用/删除，计划只有"编辑" | `Planning.tsx:274-292 vs 171-183` | 计划补停用/删除（带二次确认） |
| PL5 | 与独立页「计划编辑」双入口、字段 100% 重复 | `Planning.tsx:296-322 vs 325-428` | 保留一种：列表弹窗（推荐），删除 `plan-edit` 菜单页 |
| PL6 | 空态无引导（只有"暂无记录"） | `Planning.tsx:257-294` | 空态给"从模板新建计划"按钮 |

### 3.2 计划编辑 `plan-edit`

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| PE1 | 与计划弹窗重复（见 PL5） | `Planning.tsx:325-428` | 合并 |
| PE2 | 保存前看不到"这个计划巡检哪些点位/检测项" | `Planning.tsx:337-345` | 选中模板后就地展开点位清单预览 |
| PE3 | `提前生成 / 执行超时 / 允许等待` 属实施参数，直接给业主填 | `Planning.tsx:374,382,390` | 收进"高级设置"折叠区，默认隐藏 |
| PE4 | 无字段校验（名称、有效期先后、时间关系） | `Planning.tsx:333-398` | 前端校验 + 就地错误定位 |
| PE5 | 取消/保存裸放，无按钮分组与草稿提示 | `Planning.tsx:402-425` | 右对齐 `modal-actions` 样式；离开前提示未保存 |

### 3.3 任务列表 `tasks`

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| TK1 | 8 列偏多，且「版本快照 `m1 / p2`」是内部编码 | `Planning.tsx:564-573,591` | 合并列（如"来源"并入任务名小字）；版本改「地图 v1 / 点位 p2」并加 `title` |
| TK2 | 4 个下拉 + 搜索 + 新建按钮全挤在面板头一行，窄屏换行且"新建"与筛选混排 | `Planning.tsx:492-560` | 筛选移到表格上方独立筛选条；新建按钮独立右侧 |
| TK3 | 每页 5 条偏少（8 列表格翻页成本高） | `Planning.tsx:453` | 默认 10~20 条，并支持每页条数切换（见 G10） |
| TK4 | 「完成」列 `done/items` 无表头语义 | `Planning.tsx:592-597` | 改为「有效检测项 2 / 3」 |
| TK5 | 解释性 Note 放在表格下方 | `Planning.tsx:610-612` | 上移到表头 hint |
| TK6 | 任务名/编号是死链，无法下钻任务详情 | `Planning.tsx:574-580` | 接入 `ObjectLink to="task-detail"` |

### 3.4 目标与要求 `quick`（侧边栏已隐藏）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| QK1 | 无任务名称/优先级输入控件（`name`/`priority` 状态声明了但没有 UI），任务名恒为"临时巡检" | `Planning.tsx:12-14 vs 429-450` | 补名称与优先级输入；或删除无用状态 |
| QK2 | 勾选表无搜索/分页/全选，而 `pickQuery`/`pickPage` 状态已声明未使用 | `Planning.tsx:17-18,71-89` | 补齐工具条（顺手清死状态） |
| QK3 | 与「巡检任务日历」互为唯一入口（都是隐藏页），可达性差 | `OperationsLegacy.tsx:153` | 给一个可见入口（如工作台/任务列表的"临时任务"） |
| QK4 | 跳转前无"已选 N 项 / 预计耗时"确认 | `Planning.tsx:433-448` | 底部固定摘要 + 校验空选 |

### 3.5 巡检模板 `templates`

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| TP1 | 首行用 `s.templates[0]` 初始化 → **模板为空时整页白屏** | `Planning.tsx:10,13` | 加空值保护 + 空态引导（P0） |
| TP2 | 删除用原生 `window.confirm`，风格与全局 `Modal` 不一致 | `Planning.tsx:112` | 统一用 `Modal` 二次确认 |
| TP3 | 行内 3 个按钮（编辑/启用停用/删除）无主次，误触成本高 | `Planning.tsx:171-183` | 主操作 + "更多"折叠；删除独立且 danger |
| TP4 | Badge 手写 `className="badge ..."` 与应用 `<Badge>` 混用 | `Planning.tsx:154-170` | 统一用 `<Badge tone>`（依赖 G1） |
| TP5 | 「巡检点」只显示 `N 个`，看不到具体点位 | `Planning.tsx:166` | 显示前 2 个 + "等 N 个"，或就地展开 |
| TP6 | 弹窗"结束动作"是只读 input，看起来可编辑 | `Planning.tsx:220-222` | 改纯文本展示 |

### 3.6 调度工作台 `dispatch`（`pages/Scheduling.tsx`）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| DP1 | 任务卡/机器人卡**明细全量常驻**（注释写"选中才展开"，但 CSS 无对应规则），池子被拉长、扫读慢 | `Scheduling.tsx:323-348,410-443`；`styles.css:2182,2226` | 补 `.selected` 展开规则或默认折叠 + 显式展开按钮 |
| DP2 | 卡片明细暴露 `原子动作 / 需要能力 / 版本 m1 / p2` | `Scheduling.tsx:333-346` | 中文映射（见 G3） |
| DP3 | 顶部 7 个 KPI + 一整条全宽"统计口径"说明占满首屏 | `Scheduling.tsx:66-129` | 口径说明收进 `title`/折叠；KPI 精简为 4 项核心 |
| DP4 | "派单方式"分段控件与主按钮文案重复表达同一件事 | `Scheduling.tsx:138-149,162` | 只保留分段控件 + 中性按钮"派单" |
| DP5 | 页面过长，同一批数据出现 3 次（时间轴 / 分配记录表 / 队列） | `Scheduling.tsx:257-453,564-592,610-645` | 分区折叠 + 锚点导航 |
| DP6 | 「实际执行门槛」把多条约束 `join("；")` 塞进单元格 | `Scheduling.tsx:624-631` | 只显示首条 + "共 N 项"展开 |
| DP7 | 任务池空态无文案 | `Scheduling.tsx:273-351` | 加"待调度任务已全部派单" |
| DP8 | 派单按钮禁用原因只在 `title` 里 | `Scheduling.tsx:155-163` | 禁用时就地显示原因（与 G5 同源） |

### 3.7 机器人任务队列 `queue`

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| QU1 | 约束校验块与 dispatch 页渲染同一段 JSX，两页职责边界模糊 | `Scheduling.tsx:207-254` | 抽公共组件并明确：dispatch 只做派单、queue 只做队列管理 |
| QU2 | 提示"可拖动未下发任务"，但只有 `已分配` 可拖，文案与实现不符 | `Scheduling.tsx:478 vs 523` | 改文案或放开范围 |
| QU3 | 整行 `onClick` 选中 + 拖拽手柄 + 独立"选中"按钮，三者重复/易误触 | `Scheduling.tsx:516-553` | 去掉"选中"按钮；拖拽区与点击区分离 |
| QU4 | 「前置/后续任务」只显示任务 ID | `Scheduling.tsx:597-601` | 显示任务名 |
| QU5 | 时间粒度（4/8 小时）在页面两处开关、共用状态，无联动说明 | `Scheduling.tsx:484-495 vs 568-578` | 收敛为一个开关 |
| QU6 | 空队列提示"可从调度工作台分配"，但本页无直达入口 | `Scheduling.tsx:557-559` | 空态加按钮跳 dispatch |

### 3.8 实时执行监控 `execution`（`pages/ExecutionWorkbench.tsx`）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| EX1 | 无在跑任务时回退到 `tasks[0]` 渲染历史任务，同时下方又显示"没有执行中任务"，自相矛盾 | `ExecutionWorkbench.tsx:39-43 vs 241-248` | 无在跑任务就只显示空态（P0） |
| EX2 | 地图图例把颜色写进文字（`● 蓝色当前目标`），不渲染实际色点 | `ExecutionWorkbench.tsx:167-169` | 用色块 `<i/>` 渲染（与 MapCanvas 图例一并统一） |
| EX3 | 「任务控制」把"＋下发临时任务"与当前任务的暂停/终止/接管混在一行 | `ExecutionWorkbench.tsx:249-301` | 分组：左"当前任务控制"、右"新建临时任务" |
| EX4 | 终止任务/重试/跳过无二次确认 | `ExecutionWorkbench.tsx:285-298` | 加确认（G5） |
| EX5 | 检测项状态用 `t.failure || "执行中"` 兜底，把"失败原因"当状态显示 | `ExecutionWorkbench.tsx:210-218` | 状态与原因分列 |
| EX6 | 「演示事件注入」默认展开，6 个原始故障原因 + `TASK_FINISHED` 英文枚举占首屏 | `ExecutionWorkbench.tsx:323-367` | 默认折叠、移到底部，动作名中文化 |
| EX7 | 临时任务弹窗点位表无搜索/筛选，未启用点位仅置灰无原因 | `ExecutionWorkbench.tsx:500-527` | 加筛选 + 置灰原因 |

### 3.9 执行回溯记录 `replay`（`pages/ObjectDetails.tsx`）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| RP1 | 左侧已结束任务列表与"任务列表"页信息重叠，但少了筛选维度（无来源/机器人） | `ObjectDetails.tsx:83-208` | 补来源/机器人筛选，或复用任务列表组件 |
| RP2 | 右侧详情只有"结束信息 + 时间轴"，缺结果/告警入口 | `ObjectDetails.tsx:209-225` | 补"查看结果""关联告警"入口 |

### 3.10 执行回溯内置页 `replay-detail`（设备档案目录下）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| RD1 | 与 `replay` 页右侧详情职责重叠（一个读单任务、一个主从列表） | `ObjectDetails.tsx` `page="replay-detail"` | 明确：`replay-detail` 从业务上下文进入看单任务；`replay` 做全局检索 |
| RD2 | 失败原因只在底部小字 Note | 同上 | 提到上下文条，最显眼处 |

---

## 四、结果与异常

### 4.1 巡检结果查询 `results`（`pages/Results.tsx`）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| RS1 | 结果 ID / 点位 / 设备均为死链，无法下钻到结果详情 | `Results.tsx` 明细表 | 结果 ID 接 `to="result-detail"`，点位接 `to="point"`（G14） |
| RS2 | 筛选器已 6 项（关键词/任务/机器/状态/业务判断/日期），首屏拥挤 | `Results.tsx` 筛选栏 | 保留常用 3 项，其余收进"更多筛选" |
| RS3 | 分页单位从"结果"变成"任务"，与用户直觉不一致 | `Results.tsx` Pager | 明示"共 N 个任务"（已做）；或提供"按任务/平铺"视图切换 |
| RS4 | 任务行展开后无"全选/批量复核"能力 | `Results.tsx` | 待复核批量处理（配合 W1） |

### 4.2 结果详情 / 复核 `review` 与结果详情 `result-detail`

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| RV1 | 两页内容大量重叠（证据 / 值链路 / 复核历史 / 关联告警） | `Results.tsx` `review` vs `result-detail` | 合并为一页：只读详情区 + 复核操作区（按权限显隐），去掉重复实现（P1） |
| RV2 | 复核依据为必填但无字数/模板引导 | `Results.tsx` 人工复核 | 提供常用理由模板下拉，减少手输 |
| RV3 | "标记无效"会重算任务有效项，但界面未预警影响范围 | `Results.tsx` | 按钮旁提示"A/B 项将被排除" + 二次确认 |

### 4.3 设备巡检档案 `archive`（`pages/DeviceArchive.tsx`）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| AR1 | 5 个 tab 的表格列各写各的，风格与列命名不统一（检测对象/历史结果/采集证据/关联告警/巡检任务） | `DeviceArchive.tsx` 各 tab | 统一"时间/对象/值/判断/操作"列序与命名 |
| AR2 | 「历史结果」「采集证据」两 tab 仅第 3 列取值不同，可合并为一 tab + 视图切换 | `DeviceArchive.tsx:108-120` | 合并 |
| AR3 | 历史结果/采集证据无分页（数据增长后不可用） | `DeviceArchive.tsx:110-119` | 加 `Pager`（任务表已加） |
| AR4 | 与「设备资源树」（`equipment`）职责重叠：同一设备两个页面 | `DeviceArchive.tsx` vs `Supporting.tsx` equipment | 明确：档案看"该设备的历史业务数据"，资源树看"资产与点位结构"；或合并 |
| AR5 | 顶部设备下拉 + 统计条信息密度低，缺"最近一次巡检时间"等关键指标 | `DeviceArchive.tsx:19-44` | 补 2~3 个关键指标 |

### 4.4 告警事件 `alarms` / 告警详情 `alarm` / 告警详情内置页 `alarm-detail`

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| AL1 | 告警列表无关键词搜索、无时间范围、无点位/设备筛选 | `Results.tsx` `Alarms` 列表 | 与 `results` 页对齐补筛选（关键词 + 等级 + 时间） |
| AL2 | 列表只有"处置 / 复查"按钮，告警名/点位是死链 | 同上 | 告警名接 `to="alarm-detail"`（G14） |
| AL3 | `alarm`（处置工作台）与 `alarm-detail`（只读详情）内容重叠 | `Results.tsx` | 同 RV1：一处只读 + 一处操作，或合并 |
| AL4 | 处置轨迹只有纯文本追加，无操作人/时间结构 | `Results.tsx:474-478` | 改结构化时间线（时间 + 人 + 动作） |
| AL5 | 「创建复查任务」跳调度但未带告警上下文 | `Results.tsx:437-450` | 跳转后高亮来源告警 |

---

## 五、资源与地图

### 5.1 设备资源树 `equipment`（`pages/Supporting.tsx`）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| EQ1 | 树只有"区域 → 设备"两级，装置/罐区层级缺失 | `Supporting.tsx` `page="equipment"` | 由 `points.device` 无法推出装置层级，建议在数据模型补 `region/unit` 或引入设备主数据 |
| EQ2 | 与设备档案（`archive`）职责重叠（见 AR4） | 同上 | 见 AR4 |
| EQ3 | 设备"状态/点位启用"是派生值，无真实设备状态 | 同上 | 接入设备主数据或明确标注"按点位统计" |
| EQ4 | 台账无"关联任务/最近巡检"入口 | 同上 | 行内加"巡检档案"跳 `archive` |

### 5.2 巡检点列表 `points`（`pages/Supporting.tsx`）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| PT1 | 「编辑标注」只带 `mapId`，进入标注工作台后还需自己找点位 | `Supporting.tsx` points 表 | 传 `pointId` 并自动选中该候选目标 |
| PT2 | 无批量能力（批量启用/停用/重新验证） | 同上 | 表格加多选与批量操作条 |
| PT3 | 「状态 / 版本」把徽标与 `v1` 挤在一格 | 同上 | 拆两列或改用「已启用 · v1」统一文案 |

### 5.3 点位标注与验证工作台 `annotation`（`pages/Annotation.tsx`）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| AN1 | 「所属设备」「对象 / 检测项」是**硬编码选项**，与数据模型不一致 | `Annotation.tsx:121-132` | 选项取自 `s.points` / 设备集合 |
| AN2 | 保存时把「检测项」写进 `object` 字段（`object: item === "压力读数" ? "出口压力表" : item`），语义错位 | `Annotation.tsx:150-157` | 明确 object 与 item 两个下拉，不再推导 |
| AN3 | 切换地图只重置候选，`name/device/item/req` 残留上一张图的值 | `Annotation.tsx:40-43` | 切换时重置表单 |
| AN4 | 「剔除误识别」的禁用条件与文案不直观 | `Annotation.tsx:164-171` | 加说明 tooltip |
| AN5 | 与地图管理页互相跳转（标注→发布检查→地图；地图→点位标注），主入口不明确 | `Annotation.tsx:55-58` | 明确"标注"为主流程，地图页只读展示 |

### 5.4 地图管理 `maps`（`pages/Maps.tsx`）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| MP1 | `原始资料` tab 的"校验"列点云与视频**共用同一个 checksum** | `Maps.tsx:172-189` | 校验值分列，或标注"共用批次校验" |
| MP2 | `sync` tab「实际版本」显示 `R01 / m2 / p6`，与其它列 `m3 / p7` 格式不一致 | `Maps.tsx:234-246` | 统一格式 |
| MP3 | tab 用 URL 参数，从侧边栏进入会回到默认 `list`（其它页用 `useViewState` 存 tab） | `Maps.tsx:16,39-62` | 统一 tab 持久化策略 |
| MP4 | 列表"操作"里"业务标注"已被移除（去重），但详情页仍有"点位标注"——入口应再补一处到标注工作台的直接跳转 | `Maps.tsx:96-99,119` | 保持单一清晰入口 |
| MP5 | 与「机器人管理 › 地图与版本」职责重叠（都在讲版本同步） | `Maps.tsx` vs `Operations.tsx` robot-map | 明确：地图页管"地图资产与发布"，机器人页管"该机器实际激活版本" |

---

## 六、机器人管理

### 6.1 机器台账 `robots`（`pages/Operations.tsx`）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| RB1 | 机器人明细分4处（台账/运行监测/能力健康/机型专项），信息重复，用户需自行拼装 | `Operations.tsx` 多分支 | 合并为一个详情页 + 顶部 tab |
| RB2 | 台账缺"累计任务/巡检里程/最近故障"等运营指标 | `Operations.tsx` robots | 补 2~3 列 |
| RB3 | `OperationsLegacy.tsx` 的 `robots`/`robot`/`health`/默认分支已是死代码（仅 `calendar` 在用） | `OperationsLegacy.tsx:10-147,187-270` | 删除死分支，只保留 calendar（或把 calendar 迁到新实现后整文件删除） |

### 6.2 机器人运行监测 `robot`

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| RB4 | 运行监测 tab 的地图 + 视频与「实时执行监控」页重叠 | `Operations.tsx:398-457` vs `ExecutionWorkbench.tsx` | 监测页只保留"该机器人视角"，执行监控页做"任务视角"，并在标题写明差异 |
| RB5 | 历史任务的"过程回放"已接内置页（好），但"巡检结果"无入口 | `Operations.tsx` 历史任务表 | 结果列接 `to="result-detail"` 或跳 `results` 并预筛任务 |
| RB6 | 顶部摘要条信息密集且无分组（状态/电量/任务/版本/预计可用） | `Operations.tsx:359-382` | 分「身份 / 状态 / 当前任务」三组，用分隔符或小标题 |

### 6.3 人工操作与遥控 `manual`（已合并远程操控台）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| MN1 | 控制台 `context-bar` 不含机器人名称/编号，接管时核对对象困难 | `Execution.tsx:27-44` | 补机器人名称 |
| MN2 | 「持有人」用当前登录角色冒充会话持有人 | `Execution.tsx:65` | `Session` 结构补 `holder` 字段 |
| MN3 | 方向键 5 个按钮 3 列平铺，不是方向盘布局 | `Execution.tsx:78-89` | 改方向盘式布局（↑ 上、← → 左右、↓ 下、中心停止） |
| MN4 | 「退出人工接管」与「释放控制权」两个近义术语并存 | `Execution.tsx:104-136` | 保留一个（建议"结束接管"），另一处改动作说明 |
| MN5 | "释放控制权"未受"恢复核对"复选框约束，点了才报错 | `Execution.tsx:119-136` | 未勾选时按钮禁用 + 原因 |
| MN6 | 审计表硬截 15 条，无分页/筛选，动作显示英文码 | `Execution.tsx:151-161` | 加筛选与分页，动作中文化（G3） |
| MN7 | 地图用该地图全部点位，与执行监控页用当前任务点位口径不一致 | `Execution.tsx:51-56` | 统一口径（建议高亮当前任务点位） |

### 6.4 能力与健康 `health` / 地图与版本 `robot-map` / 机型专项 `device`

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| HE1 | 能力可用性用固定三档 + 硬编码判断（"声音"永远"未接入"） | `Operations.tsx:246-280` | 接入真实健康数据或在标题注明"示意" |
| HE2 | 页面顶部都复用同一个机器人选择器，但切换后不改变 URL，无法分享/返回特定机器人 | `Operations.tsx:217-243` | 选择机器人时同步 URL（`go(page, robotId)`） |

---

## 七、分析与报表

### 7.1 指标中心 `metrics`（`pages/Metrics.tsx`）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| MT1 | 同一指标在卡片 / 口径表 / 清单表三处重复 | `Metrics.tsx:46-137` | 卡片做概览，清单表做明细，口径表只在展开时出现 |
| MT2 | **"达标/未达标"两态都是灰**（依赖 G1），指标好坏无法一眼看出 | `Metrics.tsx:68,127` | 先修 G1，再用绿/红明确表达 |
| MT3 | 卡片是 `<button>` 但无展开态提示（无箭头/无 `aria-expanded`） | `Metrics.tsx:51-53` | 加展开图标与 `aria-expanded` |
| MT4 | 单位用 `<small>` 紧跟数值，网格中数值列对不齐 | `Metrics.tsx:57-60` | 数值右对齐 + 单位固定宽度 |
| MT5 | 清单表"操作"列不可下钻时给「—」，与"可下钻维度"列信息重复 | `Metrics.tsx:132-134` | 去掉该列，改行尾箭头 |
| MT6 | 口径配置表两行"说明"文案完全相同 | `Metrics.tsx:149,154` | 合并或改差异化说明 |

### 7.2 基础分析 `analytics`（`Supporting.tsx` 兜底分支）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| AN6 | 该分支与 `archive` 共用一套"任务与巡检质量"统计，且用 `<progress>` 原生条，与全站图表风格不一致 | `Supporting.tsx` `analytics/archive` 分支 | 用统一图表组件；`archive` 分支是死代码（App 已路由到 DeviceArchive），应删除 |
| AN7 | 压力趋势图把结果按 `unit === "MPa"` 过滤，纵坐标无刻度 | `Supporting.tsx:284-324` | 用 `ResultTrend` 统一实现（同时修 G11） |

---

## 八、系统设置

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| ST1 | `settings / services / standards / rules / routes / users` 等页共用**同一张通用兜底表**（列：编号 / 名称 / 配置·业务范围 / 状态 / 操作），语义与"字典参数""权限""标准"都不匹配 | `Supporting.tsx` 兜底分支 | 按业务域拆分字段；未实现页明确标注"规划中"，不要用同一张假表冒充 |
| ST2 | 兜底表的"＋新增 / 编辑 / 停用"对多数域不成立（如"巡检标准"应是版本化文档） | 同上 | 按域设计；或先只读展示 |
| ST3 | `audit`（审计日志）与 `interface-log` 共用日志表，但列名（"事件/业务对象"）对接口日志不贴切 | `Supporting.tsx:62-115` | 按域调整列名与筛选 |
| ST4 | 角色权限矩阵 9 列 + 12 行，横向滚动、信息重复、"当前身份/已选中"两套选中语义 | `RoleMatrix.tsx:34-56` | 拆为"角色列表 + 详情"，去掉重复列 |

---

## 九、驾驶舱（大屏）

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| CK1 | **大屏没有退出入口**（顶栏"返回工作台"被整体注释掉） | `ScreenTopBar.tsx:76-83` | 恢复返回按钮或加"退出大屏" |
| CK2 | 顶栏品牌/标题是占位符「XXX智能机器巡检平台」 | `ScreenTopBar.tsx:22,53` | 替换为真实名称 |
| CK3 | 口径说明按钮点开后结果在页面最底部，像"没反应" | `MonitorCockpit.tsx:202-205,507-533` | 改抽屉/弹窗，或点击后平滑滚动定位 |
| CK4 | KPI 条与下方卡片重复展示同批指标 | `MonitorCockpit.tsx:165-206 vs 434-504` | 见 G9 |
| CK5 | 环形/趋势图无纵轴刻度、无单位、无阈值线；告警趋势恒传 `ok` 导致永不告警色 | `MonitorCockpit.tsx:395`、`ResultTrend.tsx` | 见 G11 |
| CK6 | 告警列表 `slice(0,4)` 截断无"查看全部/还有 N 条" | `MonitorCockpit.tsx:371`、`DispatchCockpit.tsx:506` | 补截断提示 |
| CK7 | 调度驾驶舱 KPI 8 项单行拥挤，全部只读不可下钻 | `DispatchCockpit.tsx:117-199` | 精简为 5~6 项核心 + 可下钻 |
| CK8 | 「BIM 视图」是死按钮（`onClick={() => undefined}`） | `DispatchCockpit.tsx:360-365` | 见 G2 |
| CK9 | 急停第二复核人写死"李主管"，且急停未长按/二次确认即可触达弹窗 | `DispatchCockpit.tsx:50,184-186` | 复核人可选；急停改长按或二次确认 |
| CK10 | 云台 4 按钮纯文字、禁用仅置灰不解释原因 | `DispatchCockpit.tsx:458-493` | 加图标 + 禁用原因 |
| CK11 | 大屏切换 Tab 会重置两侧驾驶舱的全部筛选状态，且无法用 URL 分享/回退 | `CockpitScreen.tsx:16-20` | 关键筛选写入 URL query |

---

## 十、共用组件层

| 编号 | 优化点 | 位置 | 建议 |
|------|--------|------|------|
| CP1 | `Badge` 颜色靠正则猜文案（G1）；`ObjectLink` 默认不可点，全站"看起来能点其实点不动" | `UI.tsx:Badge`、`Business.tsx:ObjectLink` | Badge 加 `tone`；ObjectLink 对已实现内置页默认给 `to` |
| CP2 | `Video` 用 label 正则决定画哪种仪表（热像/阀门/压力表），传错就画错 | `Video.tsx:10-11,64` | 改成显式 `kind` 入参 |
| CP3 | `MapCanvas` 图例只有 3 色，实际点位有 5~6 种状态色；机器人标签全拼 `id+机型+状态` 会重叠 | `MapCanvas.tsx:169-183,299-325` | 图例与状态色对齐；标签按缩放抽稀 |
| CP4 | 缩放把文字/线宽一起放大，且无平移边界、无缩放百分比反馈 | `MapCanvas.tsx:43-45,77-79` | 文字反向缩放；加百分比与边界限制 |
| CP5 | `ControlPad` 只有点击脉冲，无"按住-持续/松开-停止"，与真实遥控习惯不符，也无键盘支持 | `ControlPad.tsx:37-71` | 加握持交互与方向键支持 |
| CP6 | `Table` 无排序、无空态自定义；`Pager` 无页码与每页条数 | `UI.tsx:72-102`、`Business.tsx:Pager` | 见 G10 |

---

## 十一、建议排期（供参考）

| 阶段 | 内容 | 预计收益 |
|------|------|----------|
| 第 1 批（P0） | G1 徽标语义、G2 假可点击、G5 危险操作确认、G7/TP1 空态白屏、W1 工作台原地处理、EX1 执行监控空态矛盾 | 消除"看起来能用其实不能用"和误导性信息 |
| 第 2 批（P1） | G3/G4 术语统一、G8 去重复入口、G9 指标去重、G10 表格能力、G11 图表可读性、G12 权限反馈、各页筛选/分页补齐 | 信息层级与一致性大幅提升 |
| 第 3 批（P2） | 布局与视觉打磨（密度、对齐、间距）、CK 系列大屏细节、CP 组件体验 | 质感提升 |

---

## 十二、附：本次已修复、无需再做的项

（2026-09-26 前的改造，供后续核对，避免重复排期）

- 工作台：由「KPI + 平铺看板」改为「KPI 点击 → 明细弹窗」，页面只读展示、复核等写操作跳转对应页面（`Workbench.tsx`、`styles.css` 清理看板样式）。
- 结果模块：按任务分组可折叠、任务/机器筛选、任务判断凸显、时间维度（`Results.tsx`、`DeviceArchive.tsx`）。
- 内置页体系：`result-detail` / `alarm-detail` / `replay-detail`，并支持 `?from=` 动态面包屑（`navigation.ts`、`App.tsx`、`Business.tsx`）。
- 远程操控台已合并进「人工操作与遥控」，`control` 独立页与路由已删除。
- 资源与地图：巡检点列表补筛选分页、设备资源页统一数据源与去掉通用 CRUD、删除 `objects` 重复页。
- 离线分发：`scripts/export-static.mjs` 可产出双击即用的自包含 `index.html`。

---

*文档结束 · 页面优化清单-01*
