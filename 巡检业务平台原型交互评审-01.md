# 巡检业务平台原型 · 交互评审报告

| 项目 | 内容 |
|------|------|
| 文档名称 | 巡检业务平台原型交互评审-01 |
| 评审对象 | `d:\traeFile\AI对话\9_23\巡检业务平台原型` |
| 技术栈 | React + TypeScript + Vite（`src/` 下 22 个 `.tsx` + 5 个 `.ts`） |
| 评审视角 | 业主侧使用者（装置/罐区业务管理员、值班员） |
| 评审日期 | 2026-09-23 |
| 结论 | 可操作性不足，主要问题在**信息架构**层面，非视觉层面 |
| 落地状态 | 见文末「六、落地回访（2026-09-26）」——P0 建议基本已落地 |

> ⚠️ **阅读提示**：本报告是 **2026-09-23 首轮评审的原始记录**，其中的"13 个一级中心 / 40 个页面 / 行号 / 待改造清单"均反映**当时**状态，后续项目做过大量结构调整（一级中心已合并、内置页体系已建立、部分页面已删除或合并）。原文保留不改（作为历史基线），
> **最新页面与文件映射请以 `PAGE_MAP.md` 为准**；逐页 UI/UX 待办事项见 `页面优化清单-01.md`。

---

## 一、结论摘要

原型功能覆盖度是够的（导航、地图、调度、执行、结果、告警、档案、系统管理均有页面），但**从业主视角上手成本偏高**，用户的直觉判断成立。

问题集中在三个方面：

1. **菜单按厂商能力域切分**，不是按业主业务任务切分；
2. **核心业务闭环（发现异常 → 处理 → 复查 → 关闭）被拆散到 4 个一级中心**，路径过长；
3. **技术术语与内部编码直接暴露**，且存在误操作风险点。

下文逐条给出证据（文件 + 行号）与改造建议。

---

## 二、问题清单与证据

### 2.1 【严重】菜单按"能力域"划分，共 13 个一级中心、40 个页面

`src/data/navigation.ts:2-54` 定义了 13 个一级分组：

```
运行监控 / 机器人管理 / 巡检资源 / 计划任务 / 调度中心 / 执行中心 /
结果中心 / 识别服务 / 异常告警 / 数据分析 / 媒体资源 / 开放集成 / 系统管理
```

其中 `运行监控 / 机器人管理 / 巡检资源 / 调度中心 / 识别服务 / 媒体资源 / 开放集成` 
属于**实施方与开发方的能力划分**，不是业主的思维划分。

业主脑子里的分类只有三类：

- 今天要巡检什么
- 机器人现在干到哪了
- 出了异常谁去处理

**页面总量**：`routeMeta` 共 40 项（38 个配置页面 + 2 个隐藏详情 `point`、`task-detail`），
其中标注 `P1` 的占位页面有 10 个，点进去无实际内容，业主第一印象为"系统不可用"。

P1 页面清单：`calendar`、`health`、`routes`、`dispatch-log`、`replay`、`archive`、
`services`、`rules`、`analytics`、`media`。

### 2.2 【严重】异常闭环跨 4 个中心，路径 6 跳以上

业主最高频主线：**看到异常 → 确认 → 安排复查 → 看复查结果 → 关闭**。

现有路径：

| 步骤 | 操作 | 所在中心 | 代码位置 |
|------|------|----------|----------|
| 1 | 结果查询，翻列表找"待复核" | 结果中心 | `Results.tsx:99-144` |
| 2 | 点「证据 / 复核」进入 review | 结果中心 | `Results.tsx:140` |
| 3 | 点「处置与复查 →」跳告警详情 | 结果中心 → 异常告警 | `Results.tsx:291-304` |
| 4 | 点「创建复查任务」跳调度 | 异常告警 → 调度中心 | `Results.tsx:426-439` |
| 5 | 点「立即下发」并跳执行监控 | 调度中心 → 执行中心 | `Scheduling.tsx:119-125` |
| 6 | 复查结束后回结果中心查结果 | 结果中心 | — |

**问题放大点**：
- "待复核结果"与"待确认告警"两个待办分居两个中心，没有统一收口；
- 首页驾驶舱仅在「需要值班人员关注」里混排，且最多显示 8 条（`BigScreen.tsx:393`），
  关掉后再也找不回来；
- 结果查询默认筛选为"全部"（`Results.tsx:11-15`），不聚焦待办。

### 2.3 【严重】技术术语与内部编码对业主暴露

| 暴露内容 | 位置 | 业主理解成本 |
|----------|------|--------------|
| `提前生成 / 执行超时 / 允许等待`（调度算法参数） | `Planning.tsx:220-243` | 属实施参数，不应让业主填 |
| 原 `加入队列 / 立即下发 / 模拟接收回执 / 置于队首 / 任务抢占` 七按钮集群（两页重复） | `Scheduling.tsx`（已重构） | 队列算法概念，非业务语言 → 已优化：调度工作台收敛为单一「派单」动作（加入队列 / 立即执行 切换）；管理动作（改派 / 置于队首 / 任务抢占 / 暂停恢复 / 移除）仅保留在「机器人任务队列」页 |
| 表格列 `版本快照 m1 / p1` | `Planning.tsx:356` | 地图版本 + 点位集编码 |
| `targetId`、`点位集 pointSet`、`原子动作` | `types.ts:13-92` 派生展示 | 内部数据模型字段 |
| `V1.4 一期设计基线` | `App.tsx:163` | 与业主无关的版本标记 |
| `● Mock 演示环境` | `App.tsx:184`、`BigScreen.tsx:219` | 易被质疑"演示即造假" |

### 2.4 【中】误操作风险

首页驾驶舱右侧把"监控信息"与"操作岗动作"放在同一视觉层级：

```tsx
// BigScreen.tsx:405-457
<section className="screen-card">
  <h3>导航控制 · {robot?.id || "—"}</h3>
   单点导航 / 自动回充 / 启用键盘遥控
</section>
<section className="screen-card">
  <h3>姿态动作 · {robot?.id || "—"}</h3>
   蹲下 / 站立
</section>
```

- `蹲下 / 站立 / 键盘遥控 / 自动回充` 属操作岗职责，业主不参与，但位置显眼易误点；
- `任务抢占` 属高影响操作，已从调度工作台移除、仅保留在「机器人任务队列」页，并复用 reassign/preempt 二次确认弹窗，确认影响后再执行；
- 「紧急停止」有二次确认弹窗（`BigScreen.tsx:460-475`），该项处理是正确的，可作参考范式。

### 2.5 【中】交互细节摩擦点

| 序号 | 问题 | 位置 |
|------|------|------|
| 1 | KPI 数字按 `disabled={!clickable}` 区分，但可点/不可点外观一致，点击无反馈 | `BigScreen.tsx:262-267`、`BigScreen.tsx:104-131` |
| 2 | 筛选条件写入 `sessionStorage`，业主再次进入看到上次筛选结果，易误判"数据丢了" | `Results.tsx:11-15`、`navigation.ts:230-253` |
| 3 | 分页仅"上一页/下一页"，无页码、无每页条数；表格不支持排序 | `Business.tsx:181-206` |
| 4 | 详情页存在 4 层头部（面包屑 + 标题块 + 二级 Tab + 上下文条），纵向挤压内容区 | `App.tsx:167-182`、`Business.tsx:76-126` |
| 5 | 侧边栏 13 个入口靠 `01-13` 序号 + 图标区分，记忆成本高 | `App.tsx:118-161` |
| 6 | 全局搜索仅匹配一级页面名，搜"异常""失败""待复核"均无结果 | `App.tsx:123-127` |

---

## 三、改造建议

### P0 — 结构调整（建议优先落地）

**1. 新增"我的工作台"作为默认首页**

一块屏幕收口全部待办，每条支持**原地处理**，处理后自动出列：

- 待复核结果
- 待确认告警
- 待调度任务
- 离线机器人 / 低电量机器人 / 地图版本不一致
- 即将超期任务

**2. 一级中心由 13 个压缩至 5 个**

| 新中心 | 归并内容 |
|--------|----------|
| 工作台 | 待办收口 + 驾驶舱 |
| 巡检执行 | 计划任务 + 调度中心 + 执行中心 |
| 结果与异常 | 结果中心 + 异常告警 + 设备档案 |
| 资源与地图 | 巡检资源 + 机器人管理 + 地图 |
| 系统设置 | 识别服务 + 媒体资源 + 开放集成 + 系统管理 + 审计 |

**3. P1 未实现页面从菜单隐藏**

保留代码与路由，仅从侧边栏与二级 Tab 移除，避免业主点进空页面。

### P1 — 文案改为业务语言

| 现状 | 建议 |
|------|------|
| `R01 / m1 / p1 / targetId / 点位集` | `1号机器人 / 地图v1 / 已覆盖12个点位 / 罐顶压力表` |
| `加入队列 / 立即下发 / 置于队首` | `排给机器人 / 现在开始 / 优先执行` |
| `任务抢占` | `紧急插队（将中断当前任务）` + 二次确认 |
| `提前生成 / 执行超时 / 允许等待` | 收进"高级设置"折叠区，默认隐藏 |
| `V1.4 一期设计基线` | 移除 |
| `● Mock 演示环境` | 改为 `演示数据` |

### P2 — 交互细节

1. 不可点击的 KPI 移除按钮外观；可点击项增加箭头/下划线提示；
2. 筛选条件默认值不持久化，页面内提供显眼的"重置"入口；
3. 表格增加排序与每页条数，分页增加页码；
4. 驾驶舱中的 `蹲下 / 站立 / 键盘遥控` 移至「操作 / 遥控台」页，首页只留监控信息；
5. 详情页头部压缩为 2 层（面包屑 + 上下文条）；
6. 全局搜索扩展为支持业务关键词（异常、失败、待复核、离线等）。

---

## 四、附：待改造文件清单

| 优先级 | 文件 | 改造内容 |
|--------|------|----------|
| P0 | `src/data/navigation.ts` | 一级分组压缩、P1 页面隐藏、新增"工作台"路由 |
| P0 | `src/App.tsx` | 侧边栏分组渲染适配、默认路由指向工作台、移除 Mock/版本标识 |
| P0 | `src/pages/Workbench.tsx`（新建） | 待办统一收口 + 原地处理 |
| P1 | `src/pages/Scheduling.tsx` | 按钮文案业务化、`任务抢占` 加二次确认（已落地：派单收敛为单一动作、抢占/改派走二次确认弹窗） |
| P1 | `src/pages/Planning.tsx` | 调度算法参数收进高级设置、去掉 `m/p` 编码展示 |
| P1 | `src/pages/BigScreen.tsx` | 操作岗动作外移、KPI 可点性视觉区分 |
| P2 | `src/components/Business.tsx` | 分页器增强（页码 / 每页条数） |
| P2 | `src/components/UI.tsx` | 表格排序能力 |
| P2 | `src/pages/Results.tsx` | 筛选不持久化、默认聚焦待复核 |

---

## 五、评审前置假设

本报告基于"业主 = 装置/罐区业务管理员与值班员"这一假设。

若实际所指"业主"为**项目甲方评审人员**，评审重点应转为
"核心业务路径能否在 3 分钟内演示完成"，P0 中的"工作台收口"依然成立，
但菜单合并的优先级可下调。

建议在实施前确认该角色定义，避免改造方向偏差。

---

## 六、落地回访（2026-09-26）

### 6.1 原文建议的落地情况

| 原文建议 | 现状 | 结论 |
|---------|------|------|
| P0-1 新增「我的工作台」作为默认首页，待办统一收口 | 已落地：`Workbench.tsx`，顶部 KPI 条 + 5 块待办面板（待复核结果 / 待确认告警 / 待调度任务 / 机器人关注 / 即将超期） | ✅ 已完成；但**「原地处理」能力在后续重构中丢失**（复核弹窗成了死代码），已转入 `页面优化清单-01.md` W1 |
| P0-2 一级中心 13 → 5 | 已落地：现为 **7 个**（工作台 / 巡检执行 / 结果与异常 / 资源与地图 / 机器人管理 / 分析与报表 / 系统设置） | ✅ 基本完成（机器人管理因资产治理需要单独成组） |
| P0-3 未实现页面从菜单隐藏 | 已落地：`navigation.ts` 的 `hiddenIds` 统一隐藏（`calendar` / `dispatch-log` / `rules` / `routes` / `standards` / `services` / `analytics` / `media` / `users` / `quick`） | ✅ 已完成 |
| P1 文案改为业务语言 | 部分落地：`● Mock 演示环境` 已移除、队列七按钮已收敛为「派单」单动作；`m1 / p1`、`原子动作`、`CONTROL_ACK` 等**仍存在** | ⚠️ 进行中，见清单 G3/G4 |
| P1 调度参数收进高级设置 | 未落地 | ⏳ 转入清单 PE3 |
| P2-1 不可点 KPI 去掉按钮外观 / 可点项加提示 | 部分落地：`Kpis` 已按有无 `action` 区分 `button`/`div`；但**仍有"假可点击"**（设备行、BIM 按钮等） | ⚠️ 见清单 G2 |
| P2-2 筛选默认值不持久化 + 显眼重置入口 | 未落地（筛选仍持久化在 `sessionStorage`，但已加容错） | ⏳ 见清单 RS2 |
| P2-3 表格排序 / 每页条数 / 页码 | 未落地（`Pager` 仍只有上一页 / 下一页） | ⏳ 见清单 G10 |
| P2-4 驾驶舱操作岗动作外移 | 已落地：`ScreenTopBar` 与原 `BigScreen.tsx` 均已重构；驾驶舱只保留监控与调度指挥 | ✅ 已完成 |
| P2-5 详情页头部压缩为 2 层 | 未落地（仍是 面包屑 + 上下文条 [+ 二级 Tab]） | ⏳ 见清单 W6/AR5 等页 |
| P2-6 全局搜索支持业务关键词 | 已落地：`App.tsx` 的 `keywordPages`（异常 / 告警 / 待复核 / 接管 / 离线 …） | ✅ 已完成 |

> 注：原文多处引用的 `BigScreen.tsx` 现已拆分为 `MonitorCockpit.tsx` + `DispatchCockpit.tsx` + `CockpitScreen.tsx`。

### 6.2 期间新增的重要结构变化（原文未涉及）

1. **内置页体系**：新增 `result-detail` / `alarm-detail` / `replay-detail` 三个隐藏只读详情页，并引入 `?from=page/id` 让**面包屑、返回、侧边栏高亮按入口动态归属**（`navigation.ts` 的 `Route.from` + `App.tsx` 的 `fromMeta`）。
2. **远程操控台并入「人工操作与遥控」**：删除独立 `control` 页与路由，各处「接管 / 遥控台」统一进入 `manual`（页面内嵌 `ControlConsole`）。
3. **去重复入口**：删除 `objects`（设备 → 对象 → 检测项）独立页；移除地图列表与详情重复的「业务标注」；设备资源页去掉不适用的通用 CRUD。
4. **设备资源页重做**：树与台账统一以「点位所属设备」为唯一数据源，弹窗按设备过滤。
5. **巡检点列表增强**：关键词 + 设备 / 地图 / 状态筛选 + 分页，并移除写死的「必检」列。
6. **结果模块按任务维度重构**：任务分组可折叠、任务 / 机器筛选、任务判断（正常 / 异常）凸显。
7. **设备巡检档案增强**：巡检任务表对齐任务列表口径、新增时间维度展示与筛选、「过程回放」接入执行回溯内置页。
8. **离线分发能力**：`scripts/export-static.mjs` 可产出**双击即用的自包含 `index.html`**；并给本地存储读写加了容错（`file://` 下部分浏览器禁用存储）。
9. **驾驶舱与机器人任务表**：工作台看板布局修复；机器人「历史任务」对齐任务列表口径。

### 6.3 仍未解决的原文问题（已转入《页面优化清单-01.md》）

| 原文编号 | 问题 | 清单对应条目 |
|---------|------|-------------|
| 2.3 | 内部编码 / 术语外露（`m/p`、`原子动作`、英文动作码） | G3、G4 |
| 2.4 | 误操作风险（危险动作无二次确认） | G5、EX4、MN5 |
| 2.5-3 | 分页能力弱（无页码 / 每页条数 / 排序） | G10 |
| 2.5-4 | 详情页头部层级多 | 各页"上下文条 + 面包屑"相关条目 |
| — | 新增发现：徽标颜色语义失效、假可点击、空态白屏、指标重复铺陈、图表可读性 | G1、G2、G7、G9、G11 |

---

*报告结束 · 原文保留为历史记录，落地情况见「六、落地回访」*
